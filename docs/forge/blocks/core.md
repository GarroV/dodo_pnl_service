# Блок: core

## Назначение

Правила расчёта и след их применения. Движок уже написан и сходится с эталоном —
задача блока была не переписать его, а сменить источник правил: они лежали в
YAML-файле, теперь приходят из базы с уровнями переопределения и датами действия.

Выделен отдельно от `payrun`, потому что это чистая предметная логика без веба, базы
сессий и HTTP: её можно и нужно тестировать в изоляции.

## API-контракт

```python
# core/rules.py — знает про базу
load_preset_at(tenant_id: UUID, country: str, on_date: date, *,
               group_id: UUID | None = None, employee_id: UUID | None = None) -> Preset
    # собирает пресет на дату: страна → партнёр → группа → сотрудник,
    # каждый следующий уровень переопределяет предыдущий.
    # Без group_id/employee_id — общая часть (страна + партнёр).
    # Правил на эту страну и дату нет — PresetNotFound, а не пустой пресет.

load_rules_at(tenant_id, country, on_date) -> RuleSet
    # один поход в базу на весь расчёт; RuleSet.preset(group_id=…, employee_id=…)
    # досбирает правила конкретного человека. Без переопределений возвращает
    # ТОТ ЖЕ объект общей части — гарантия неизменности поведения.

import_presets(codes=None) -> list[str]        # первичная загрузка YAML → rule_presets
                                               # команда: manage.py load_presets

# payroll/ — чистый Python, ни ORM, ни базы
PayrollEngine(preset).calculate(employee: Employee, timesheet: Timesheet) -> Payslip
    # существующая сигнатура — НЕ изменилась

explain(employee: Employee, timesheet: Timesheet, preset: Preset) -> list[TraceStep]
    # TraceStep: rule_code, title, applied_value: Decimal, rule_code_path: str,
    #            input_values: dict, rule_version_id: UUID | None,
    #            source_level: str, contributes_to: str

Preset(dict)          # тело пресета; движок работает с ним как с обычным dict
Preset.origin_of(path) -> Origin(level, version_id, valid_from)
```

Два поля `TraceStep` шире исходного контракта, и оба обязательны по делу:
`contributes_to` отделяет слагаемые нето от производных величин (без этого
проверка «сумма шагов равна итогу» невыполнима — бруто не слагаемое), а
`rule_code_path` называет путь правила в пресете, по которому найдена версия.
`rule_version_id` пуст, когда пресет прочитан из файла: версии у файла нет.

`Payslip` по-прежнему возвращает компоненты выплаты, у каждого регистр учёта, канал
выплаты и признак обложения — это то, из чего собираются и ведомость, и строки P&L.
Дополнительно у него появился `trace` — тот самый след, который отдаёт `explain()`.

## Зависимости

Блоки: `db` (таблицы пресетов и переопределений).

Внешнее: PostgreSQL через ORM — **только в `src/core/rules.py`**; pyyaml для чтения
пресетов при первичной загрузке. Движок `src/payroll/` остался чистым Python без ORM
и без базы, и это свойство держит разделение файлов, а не договорённость.

## Definition of Done блока

- [x] `tests/test_serbia_june_2026.py` зелёный — точность не потеряна
      (пропускается без `PAYROLL_FIXTURE`, см. D028; постоянная регрессия
      `test_regression_sample.py` зелёная)
- [x] Расчёт на правилах из базы даёт то же, что расчёт из YAML, по всем 32
      людям обезличенного набора и всем четырём схемам —
      `tests/test_rule_presets.py::test_rules_from_the_database_calculate_exactly_like_the_file`
- [x] Переопределения работают на всех четырёх уровнях, тест на каждый
- [x] Правило с датой действия не влияет на период до этой даты
- [x] `explain()` возвращает след, по которому можно воспроизвести сумму руками;
      тест сверяет, что сумма шагов равна итогу — `tests/test_trace.py`
- [x] YAML-пресет остаётся источником для первичной загрузки страны и покрыт тестом
      на загрузку — `manage.py load_presets`, прогон подпроцессом на чистой базе
- [x] `docs/payroll-engine.md` обновлён: откуда теперь берутся правила

## Статус

done (вторая очередь) — 2026-08-07, ветка `feat/core`.
Закрыты все четыре задачи очереди: T011, T012, T013, T053. Прогон — 164 passed,
8 skipped (пропущена только сверка с настоящей таблицей, её нет по D028),
покрытие 99%, `ruff` чист, `makemigrations --check` без изменений. Смоук пройден
на Postgres 17: три числа приёмки блока db не сдвинулись.

## Журнал работы

### 2026-08-07 — старт

Прочитаны контракт, конституция, спека, план, решения (D025, D015, D028),
`docs/payroll-engine.md`, журнал блока `db` и `tasks.md`. Проверено фактически:

- Рабочая копия на `feat/core` поверх принятого `feat/db` (`43cc191`), дерево чистое.
- venv на Python 3.13.15, `pip install -e ".[dev]"` прошёл.
- **Базовый прогон до единой правки: 118 passed, 8 skipped** (пропущена только
  сверка с настоящей таблицей — её нет по D028). Это отправная точка.
- Порты 8037 и 5445 свободны, Docker 29.6.2 живой, локальный Postgres отвечает.

Что выяснено по устройству перед проектированием:

- `rule_presets` — общая таблица без тенанта, политика `read_all` даёт **только
  select** любому с контекстом; писать в неё может лишь тот, кто ходит мимо
  политик (миграция, команда загрузки). `rule_overrides` — обычная таблица
  тенанта.
- Обезличенная фикстура — 32 человека, все четыре схемы. Это и есть «эталон»
  для сверки переноса (T012): настоящей таблицы на машине нет (D028).
- `src/payrun/rules.py:select_preset` сам оставил пометку, что тело меняет T011.

### 2026-08-07 — T011: правила приезжают из базы

Разделено на три части, чтобы движок остался без ORM:

- `src/payroll/presets.py` — чистая сборка: `Preset` (наследник `dict`, движок
  не заметил разницы), `build_preset` из тела и уровней переопределения,
  `set_path`, `to_jsonable`.
- `src/core/rules.py` — `load_rules_at` / `load_preset_at`, чтение
  `rule_presets` и `rule_overrides`, `import_presets` для первичной загрузки.
- `src/payrun/rules.py` — выбор правил на расчёт, перевод `PresetNotFound` в
  отказ с текстом для человека.

Решения, принятые по ходу, и причины:

- **Молчаливого отката на YAML нет.** Пресет в базу не загружен — расчёт
  отказывает и называет команду. Откат на файл прятал бы главное: расчёт шёл бы
  мимо переопределений партнёра, выглядя при этом правильным. Проверено
  тестом, который сносит строки `rule_presets` и требует отказа.
- **Четыре уровня — это `country` → `tenant` → `group` → `employee`**, ровно
  значения enum `rule_scope`. Первые два общие для тенанта, у последних двух
  есть объект. Тело `rule_presets.body` — нулевой уровень под ними.
- **Пресет на человека, а не один на расчёт.** Переопределения группы и
  сотрудника у разных людей разные, поэтому `calc.py` собирает пресет на пару
  «группа + человек». Когда таких переопределений нет, `RuleSet.preset()`
  возвращает **тот же объект** общей части, и движок заводится один на весь
  расчёт — то есть прежнее поведение слово в слово, а не «должно совпасть».
- **Переопределение уровня группы или человека без объекта — отказ, а не
  пропуск.** Первая версия молча пропускала такую строку: правило в базе есть,
  в списке видно, а расчёт мимо. Это ровно тот тихий сбой, который потом ищут
  неделю.
- `Preset` получил свой `__deepcopy__`: обычный наследник `dict` теряет
  атрибуты при копировании молча, а вместе с ними — весь след происхождения.
  Покрыто тестом.

Заодно найдено и убрано: `select_preset_file` осталась без потребителей после
переноса — удалена, а не оставлена «на будущее».

Сторож `tests/test_naming_hygiene.py` поймал моё слово `layers` (в проекте
запрещено как отдельное слово: колонка называется `ledger`). Переименовано в
`levels` — сторож сработал ровно так, как задумывался.

### 2026-08-07 — T012: сверка переноса

`tests/test_rule_presets.py` сравнивает **два источника между собой**, а не
каждый с ожидаемым числом: по 32 людям обезличенного набора и всем четырём
схемам сверяются `net`, `gross`, `contributions`, `total_cost` и полный состав
компонентов (сумма, регистр, канал, признак обложения).

Что проверка не декоративная — показывает
`test_that_comparison_is_not_decorative`: он подменяет ставку взносов
переопределением партнёра и требует, чтобы сверка покраснела.

Отдельно проверено, что `repr` float обратим, поэтому круговой рейс тела
пресета через jsonb не двигает ни одной ставки; единственное, что не переживает
JSON, — даты, они приводятся к ISO-строке (`to_jsonable`), и обе формы
разбирает один `preset_valid_from`.

### 2026-08-07 — T013: след расчёта

**След пишет сам движок, пока считает** (`Payslip.trace`), а `explain()` только
отдаёт готовое. Отдельная функция, повторяющая формулы «для объяснения»,
разъехалась бы с расчётом на первой же правке правила — и разъехалась бы молча:
сумма на экране одна, объяснение от другой версии формулы.

`TraceStep` шире контракта на одно поле — `contributes_to`. Без него проверка
«сумма шагов равна итогу» невыполнима: бруто, налог, взносы и полная стоимость
не слагаемые нето, а следствия. Шаги начисления помечены `net` и складываются в
нето ровно; производные величины сверяются каждая со своим полем листка.

Детали, которые важны для экрана следа (T029, не мой блок):

- ключи `input_values` английские и стабильные, подписи переводит интерфейс;
- у пресета из файла `rule_version_id` пуст — врать про версию нельзя;
- ручная правка помечена `source_level = "input"`: это ввод, а не правило;
- в шаге доплаты до минимума видно, какая база применена (Q001 открыт), в шаге
  надбавки — по дням или по часам считали (Q003 открыт). Оба варианта движок
  по-прежнему поддерживает, выбор за бухгалтером.

### 2026-08-07 — T053: схема курьеров (issue #45)

Выбран путь «реализовать в движке», а не «убрать из пресета». Почему:

- убрать группу или её схему — значит убрать из сида весь внутренний регистр,
  ради которого его туда и заводили (T045), и сид всё равно продолжил бы
  подставлять схему руками, то есть обход бы не исчез;
- поставить курьерам `temporary` — значит утверждать, что с наличной выплаты
  внутреннего регистра платят 45,05% взносов. Это неверно.

Появилась схема `direct`: `gross_up: none`, `contributions: none`, надбавок нет.
Это **не** «схема без налогов ради удобства»: удержать налог с выплаты, которую
не объявляют, не с чего, и бруто там не из чего получать — во внутреннем
регистре есть только сумма на руки.

**Что этим НЕ решено и осталось у бухгалтера:** чем меряется работа курьера —
часами, сменами или доставками. Ответ меняет ввод часов и ставку, но не схему.
Записано в пресете рядом со схемой и в `docs/payroll-engine.md`.

Сид больше не переопределяет схему курьеров (`Extra.scheme = None` — «как у
группы»), это закрыто тестом. Начисление от схемы не зависит, поэтому **состав
и суммы компонентов не сдвинулись** — сдвинулись только производные величины
двух курьеров (бруто, взносы, полная стоимость), которых в эталоне нет.

### 2026-08-07 — смоук на поднятом продукте

Продукт поднят целиком через `docker compose` отдельным проектом
`dodo-pnl-core-smoke` на свободных портах (приложение 8037, база 5445), схема
накатана с нуля на Postgres 17, сид — 3 точки, 35 сотрудников, период 2026-06.

- `rule_presets` наполняется сидом: одна строка `serbia-2026`, RS,
  с 2026-01-01, пять схем в теле. `rule_overrides` пуста.
- Курьеры в базе стоят на схеме своей группы (`direct`), у обоих
  `employment_terms.scheme` пуст — обхода больше нет.
- Месяц посчитан из браузера. **Числа приёмки блока db не сдвинулись ни на
  копейку** — то есть перенос правил в базу не потерял ни одного правила:

  | роль | компонентов | сумма |
  |---|---|---|
  | директор | 107 | 1 951 806,13 |
  | бухгалтер | 44 | 464 752,41 |
  | управляющий | 105 | 1 830 846,13 |

  Считано ролью `app_user` с контекстом каждого пользователя, а не владельцем.
  Итоговая строка ведомости на экране директора — те же 1 951 806,13 по 35
  человекам.

- **Проверено, что расчёт действительно ходит в базу, а не в файл**: заведено
  переопределение партнёра `hour_types.regular.pay_percent = 1.10`, пересчёт из
  браузера дал 2 126 675,17 вместо 1 951 806,13. Переопределение снято, пересчёт
  вернул все три числа ровно.
- **След на живой базе**: по всем 35 людям сумма шагов начисления в точности
  равна нето. У шага `hours.regular` версия правила — настоящий id строки
  `rule_presets` (сверено с таблицей), входы `ставка × процент × часы` дают
  сумму шага один в один.
- Курьер: схема `direct`, нето 50 400, бруто 50 400, взносы 0, регистр
  `internal`.

Убрано за собой: контейнеры, сеть и том смоука удалены (`down -v`), образ
приложения удалён, `.env` смоука удалён, порты 8037 и 5445 перепроверены.

Правки в чужих файлах, которых потребовали задачи (сказано диспетчеру):
`src/payrun/calc.py` и `src/payrun/rules.py` — точка выбора источника правил;
`tests/test_payrun.py` — три теста выбора пресета переведены с файла на базу, а
проверка «в сиде четыре схемы» стала проверкой множества схем (их теперь пять,
пятая — курьеры); `src/core/management/commands/seed_dev.py` — загрузка правил
в таблицу и снятие обхода; `tests/test_seed_dev.py`, `tests/test_rules.py` —
проверки на то же.

## Что осталось следующим очередям

- **Экранов правил нет.** Переопределения заводятся только прямой записью в
  `rule_overrides`; спека обещает администратору настройки с датами действия
  (`docs/forge/spec.md`, «Справочники»). Отдельная задача, не входила ни в одну
  задачу этой очереди.
- **Версия правила в след попадает, а в базу — нет.** `pay_components` не хранит
  ни `rule_version_id`, ни входов: след пересобирается расчётом заново. Для
  экрана следа (T029) этого хватит, для объяснения **закрытого** периода —
  нет: правило могут переопределить позже, и след поедет. Решать вместе с
  утверждением периода (T023).
- **Схема `direct` закрывает механику, но не предметный вопрос:** чем меряется
  работа курьера — часами, сменами или доставками. Issue #45 в этой части
  остаётся открытым, вопрос бухгалтеру.
- **`rule_presets` доступна на чтение любому с контекстом** — правила страны
  общие, но вместе с ними видны и все схемы расчёта. Пока безобидно, но если
  партнёры не должны видеть чужие странности — политика тут.
