# Блок: db

## Назначение

Схема данных и разграничение доступа. Выделен отдельно, потому что на нём держится
главный принцип продукта: изоляция тенантов и видимость регистров учёта живут в базе,
а не в коде. Любой другой блок может забыть фильтр — и должен получить пустой
результат, а не чужие данные.

Здесь же переезд с голых SQL-файлов на миграции Django и переименование регистров
учёта в нейтральные термины.

## API-контракт

Функции контекста, на которые опираются все политики и весь остальной код:

```sql
app_user_id() returns uuid
    -- текущий пользователь; читает GUC app.user_id, выставленный приложением

app_tenant_ids() returns setof uuid
    -- тенанты, где пользователь состоит

app_visible_ledgers(p_tenant uuid) returns ledger[]
    -- регистры, видимые пользователю в этом тенанте; для чужого тенанта — пустой массив
```

Правила, которые блок гарантирует наружу:

- на всех таблицах с `tenant_id` включены RLS и `force row level security`;
- политика изоляции тенанта — пермиссивная, политики видимости регистров —
  **ограничивающие** (`as restrictive`), иначе они складываются через OR и не сужают;
- приложение ходит ролью `app_user`: не владелец таблиц, без `bypassrls`;
- без выставленного контекста любая выборка возвращает ноль строк;
- периоды действия версионируемых правил не пересекаются — ограничение `EXCLUDE`
  с `btree_gist`.

Именование после переименования: тип `ledger` со значениями `official`,
`supplementary`, `internal`; колонка `ledger` вместо `layer`.

## Зависимости

Продуктовых блоков нет — это фундамент.

Внешнее: PostgreSQL 17 (образ `pgvector/pgvector:pg17`), Django 6.1 (миграции),
psycopg 3, расширение `btree_gist`.

## Definition of Done блока

- [x] Схема накатывается на чистую базу одной командой, с нуля, без ошибок —
      `manage.py migrate` (T008)
- [x] Ни одного упоминания Supabase и `auth.uid()` в схеме и миграциях. Остаётся
      файл `db/platform/supabase.sql` — он вне схемы и удаляется задачей T003
- [ ] Регистры переименованы везде — задача T004, следующая очередь
- [x] Тест изоляции тенантов: пользователь A не получает ни одной строки B —
      прогон **ролью `app_user`**, не владельцем (T009)
- [x] Тест видимости регистров: роль с доступом только к официальному не видит
      строк других регистров и их вклада в итоги (T001, сделано досрочно)
- [x] Тест: без выставленного контекста выборка пуста
- [ ] Тест: системные роли и общий справочник статей (`tenant_id is null`) видны —
      задача T002, дефект намеренно не чинился, помечен в миграции `0004_rls`
- [ ] Тест непересечения периодов действия правил — задача T010; расширение
      `btree_gist` уже ставится миграцией `0001_types`
- [x] Каждая таблица и неочевидная колонка прокомментированы в миграции
      (`0003_comments`, комментарии видны в `psql \d+`)

## Статус

done — 2026-08-07 (T008, T009, T038 закрыты; T001 закрыт досрочно; ветка `feat/db`)

## Журнал работы

### 2026-08-07 — старт

Прочитаны контракт, конституция, план, решения, `tasks.md`. Проверено фактически:

- Django 6.1 вышел (PyPI, `requires_python >=3.12`), Python 3.13.15 поставлен
  через `brew install python@3.13`, venv `.venv` в этой копии — 3.13.
- Локальный Postgres — 16.14, не 17. Для задач T008/T009/T038 разницы нет
  (`btree_gist` и `EXCLUDE` из T010 работают с 9.x). Площадка остаётся на 17.
- В `feat/db` уже закоммичены `0004_facts.sql`, `db/platform/*.sql` и фикстуры
  БД в `tests/conftest.py` — работа параллельной сессии по фактам.

Решение по `0004_facts.sql`: **не переношу**, файл оставляю на месте
(рекомендация диспетчера, задача вне первой очереди, тестов у неё нет).
Следствие, которое нужно понимать: после переезда на миграции Django
`0004_facts.sql` перестаёт применяться как есть — он ссылается на имена
ограничений и функции из старых SQL-файлов. Порт 0004 — часть задачи
модуля фактов (`docs/backlog-facts.md`, T005).

### 2026-08-07 — T008: каркас Django и переезд схемы

Сделано:

- `manage.py` в корне, проект `src/config`, приложение `src/core`. Пакеты живут
  в `src/`, движок `payroll` не тронут вовсе.
- Модели повторяют таблицы `0001_core.sql` и `0002_payroll.sql` один в один,
  включая значения по умолчанию **на стороне базы** (`db_default`): часть данных
  приезжает чистым SQL мимо ORM, и без этого вставки без `id` падали бы.
- Миграции: `0001_types` (расширения и enum-типы), `0002_initial` (таблицы),
  `0003_comments` (комментарии), `0004_rls` (функции контекста и политики),
  `0005_app_role` (роль приложения и гранты).
- Старые `0001_core.sql`, `0002_payroll.sql`, `0003_rls.sql` удалены.

Принятые решения:

- **Enum-типы Postgres оставлены нативными**, а не заменены на текст с проверкой:
  чужое значение регистра учёта должна отвергать база. Ссылка на тип из модели —
  через `core/fields.EnumField`.
- **Функции контекста переписаны с нуля** под именами из контракта:
  `app_user_id()`, `app_tenant_ids()`, `app_visible_ledgers(tenant)`.
  Привязка к Supabase не воспроизведена. Имя `app_visible_ledgers` уже новое,
  а возвращаемый тип пока `accounting_layer[]` — тип переименуется в T004.
- **`0004_facts.sql` не переносится** и не удаляется (вне очереди). После переезда
  он перестаёт применяться как есть: ссылается на имя ограничения
  `pnl_items_kind_check`, созданное старым SQL, и пересоздаёт функции контекста.
  Порт — часть работы по фактам (`docs/backlog-facts.md`).

Расхождения с прежней схемой, о которых нужно знать:

- **`on delete` на внешних ключах Django не пишет** — каскады и `restrict` он
  исполняет в Python. Прямое `delete from` в SQL теперь не каскадится. Это цена
  решения D019, а не недосмотр; в коде удаление идёт через ORM.
- `created_at` по умолчанию — `now()`, как и было (Django-шный `Now()`
  разворачивается в `statement_timestamp()`, поэтому взята функция явно).
- Имена ограничений уникальности и проверок теперь свои
  (`units_tenant_code_uniq` и т.п.), автогенерируемые имена Postgres ушли.

### 2026-08-07 — T009: роль `app_user`

Роль заводится миграцией `0005_app_role`: `nosuperuser`, `nobypassrls`, не владеет
таблицами, права только на данные. Роль групповая (`nologin`) и выдаётся тому, кто
подключается: сервис делает `set local role app_user` в транзакции запроса вместе
с контекстом пользователя. Так пароль логина остаётся делом развёртывания и в
миграцию не попадает.

Проверка на осмысленность (то, чего требует DoD задачи) сделана фактически:
переключение роли в `as_app_user` временно заменено на заглушку, прогон
`tests/test_schema_access.py` — **6 из 7 тестов доступа покраснели**
(изоляция, пустота без контекста, запрет записи в чужой тенант, три теста
видимости регистров). Зелёным остался только `test_isolation_check_is_meaningful`,
который как раз и утверждает, что владелец видит оба тенанта.

Заодно закрыт **T001**: политики видимости регистров написаны сразу
`as restrictive`, на всех таблицах с `tenant_id` — `force row level security`.
Обратный прогон подтверждает, что тест не декоративный: с пермиссивной политикой
три теста видимости падают, бухгалтер снова видит внутренний регистр.

**T002 намеренно не чинил** — это отдельная задача. Дефект (`tenant_id is null`
не проходит политику изоляции, поэтому системные роли и общий справочник статей
не видны никому) описан прямо в миграции `0004_rls`, чтобы не читался как норма.

### 2026-08-07 — T038: сид тестовых данных

`manage.py seed_dev` наполняет чистую базу из обезличенной фикстуры
`tests/fixtures/plata-sample.xlsx`: тенант `rs-dev`, юрлицо, три точки, общий
справочник статей, группы и календарь **из того же пресета, по которому считает
движок**, 32 выдуманных сотрудника с условиями найма и часами, три роли с разной
видимостью регистров и один открытый период за июнь 2026.

Идентификаторы детерминированные (`uuid5`), команда идемпотентна: тенант
пересобирается целиком, повторный прогон ничего не удваивает — покрыто тестом.
Настоящая таблица партнёра не участвует и не может (D028).

Что при этом выяснилось и на что стоит смотреть дальше:

- **В `timesheets` некуда положить `cash_payout` и `manual_correction`.** Движок
  их принимает (`ISPLATA U KES` и `KOREKCIJA DO MINIMALCA` в таблице партнёра),
  а в схеме соответствующих колонок нет — при сиде они теряются. На расчёт по
  сиду это не влияет, но при вводе часов через интерфейс потеряется ручная
  корректировка. Нужна колонка или отдельная сущность корректировок.
- **Схема расчёта задаётся листом таблицы, а не группой.** У группы `kitchen`
  в фикстуре три разные схемы, поэтому схема живёт на `employment_terms`.
- Соответствие «лист таблицы → точка» приблизительное и живёт только в сиде:
  в таблице лист означает «где и по какой схеме считают», а не пиццерию.

### 2026-08-07 — найденный по ходу дефект: массивы enum приезжали строкой

psycopg знает только встроенные типы. Колонку `accounting_layer` он отдаёт
строкой (это то, что нужно), а массив `accounting_layer[]` — **одной строкой
целиком**: `'{white,grey,black}'` вместо списка. Через ORM ровно то же самое.

Молча это не ломается — ломается позже и не здесь: `roles.visible_layers` уходит
в контракт блока `auth` как `visible_ledgers: list[str]`, и проверка «регистр
входит в видимые» работала бы по символам, ничего не сообщая.

Починено в `core/db_types.py`: типы регистрируются на каждом соединении
(сигнал `connection_created` для ORM, явный вызов в тестах). Покрыто с двух
сторон — что список приходит списком и что без регистрации это действительно
строка.

## Что осталось следующим очередям

- **T001 закрыт досрочно** — политики видимости `as restrictive` и
  `force row level security` уже в схеме, тесты есть.
- T002 (`tenant_id is null`), T003 (удалить `db/platform/supabase.sql`),
  T004 (переименование регистров), T010 (`EXCLUDE` на непересечение периодов) —
  не трогал, это следующая очередь.
- Порт `0004_facts.sql` на миграции Django — за модулем фактов.
- Колонки под `cash_payout` и ручную корректировку в `timesheets`.
- Вопрос на подумать: регистр учёта есть не только у `pay_components` и
  `allocation_rules`, но и у `employee_groups` и `employment_terms`, а
  ограничивающей политики на них нет (так было и в `0003_rls.sql`, перенесено
  как есть). Сумм это не раскрывает, но бухгалтер видит, что группа заведена
  во внутреннем регистре. Решать вместе с T004.
