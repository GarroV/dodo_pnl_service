# Блок `visual` — визуальная основа из дизайн-системы

**Зачем этот блок существует, одной фразой:** владелец не может протестировать продукт в
текущем виде, и это блокирует приёмку всей остальной работы. Значит визуал — не украшение,
а петля обратной связи.

Дословно от владельца 2026-08-18: «нам явно надо проработать визуал, сейчас весь интерфейс
вразнобой, нечитаемый». И 2026-08-21: «визуал тоже важен, потому что в текущем варианте я
даже протестить не могу».

## Что уже есть на входе

**Эталон** — папка `Дизайн-система Dodo P&L/` в корне репозитория (лежит вне git, issue #136;
это не мешает читать её, но править её содержимое **нельзя** — она источник, а не продукт):

- `tokens.css` — **165 переменных**, семантические имена: `--canvas`, `--surface`,
  `--ink`/`--ink-2`/`--ink-3`, `--line`, `--accent`, `--warn`, `--ok`, `--up`, `--err`.
  В её `CLAUDE.md` заявлено: «переносится в Django как есть».
- `components.css` — каталог компонентов.
- `Этап 2 - дизайн-система.dc.html` — компоненты во всех состояниях, с разделами: цвет,
  семантика регистров и статусов, типографика, три языка, сетка и плотность, иконки, шапка,
  кнопки, переключатели, поля, плашки версий.
- `Этап 4 - Передача.dc.html` — раздел «Как перенести в Django» и восемь принципов, каждый со
  ссылкой на конкретное место в `components.css`. **Читать обязательно: там сказано, как
  переносить, и часть принципов куплена реальными дефектами.**
- `Тёмная тема.dc.html` — та же система, другие значения токенов.

**Техграницы эталона совпадают с нашим стеком** (проверено): Django-шаблоны, серверный
рендеринг, htmx 2.x, чистый CSS без сборщика, шрифты локально через `@font-face`, иконки
inline SVG, десктоп 1280–1920, работоспособность на 768, мобильные сценарии управляющего 375.

## Что в продукте сейчас

Проверено, а не предположено:

- **CSS-архитектуры нет.** 336 строк стилей лежат прямо в `src/web/templates/web/base.html`
  внутри `<style>` (строки 29–365). Отдельный CSS-файл в продукте **один** —
  `src/timesheets/static/timesheets/grid.css`, 207 строк.
- Отсюда и «вразнобой»: каждая очередь дописывала оформление в общий инлайн-блок.

## Контракт блока

### T176 — токены как статика продукта

Перенести `tokens.css` из эталона в статику продукта и подключить в `base.html`. Значения не
изобретать: взять как есть, потому что эталон именно для этого и делался. Тёмную тему
подключить переменными (второй набор значений), но переключатель в этой задаче не делать.

Готово, когда: файл лежит в статике, подключён, отдаётся при `DEBUG=False` (в проекте есть
`tests/test_static_files.py` — добавиться в его перечень), и ни одно значение не потеряно —
число переменных в продукте совпадает с эталоном.

### T177 — стили из `base.html` переезжают в файл и на токены

Вынести инлайн-`<style>` из `base.html` в отдельный CSS продукта и переписать значения на
токены: цвета, отбивки, радиусы, типографику. Хардкод цвета в шаблонах после этой задачи
недопустим.

Готово, когда: в `base.html` нет блока `<style>`, стили лежат файлом, **все 1545 тестов
зелёные**, и в шаблонах `src/web/templates/` не осталось литеральных цветов (проверить
поиском `#[0-9a-fA-F]{3,6}` и `rgb(`).

### T178 — каркас и шапка по эталону

Привести каркас страницы и шапку к разделу 08 эталона: имя продукта, тенант, роль,
навигация. **Одна оговорка, важная:** компонент шапки в эталоне нарисован под несколько
наборов прав рядом, а владелец 2026-08-21 решил, что **у человека одна роль**. Значит шапка
показывает одну роль. Расхождение разбирается в issue #130 — не решай его сам, следуй
решению владельца.

Готово, когда: шапка и каркас совпадают с эталоном по сетке, типографике и состояниям, обход
табом виден, на 768 не рассыпается.

### T179 — числа читаются столбиком

Первый принцип эталона: «Деньги читаются столбиком — правый край, моноширинное, табличные
цифры. Ноль — цифра, отсутствие значения — прочерк. Это разные вещи, и путать их нельзя».
Применить к таблицам продукта (ведомость, P&L, расходы, счета).

Готово, когда: во всех таблицах суммы выровнены по разряду, ноль и прочерк различимы, и это
закреплено тестом на разметку хотя бы одной таблицы.

## Чего в этом блоке НЕ делать

- **Не переделывать экраны по одному.** Это основа: токены, каркас, шапка, таблицы. Остальные
  экраны переезжают по правилу владельца — «экран, которого касаемся по любой задаче,
  переезжает на систему тем же изменением».
- **Не трогать папку эталона.** Она источник; правки в неё — отдельный разговор (#136).
- **Не решать вопросы из `questions.md`** и не решать расхождение по числу ролей (#130).
- **Не менять схему данных.** Схему в этой волне ведёт другой блок.
- Не заводить сборщик, не тянуть внешние шрифты и библиотеки: у эталона прямой запрет на
  внешние загрузки в проде, а у продукта — чистый CSS без сборки.

## Журнал работы

Ведётся по ходу, а не в конце. По каждой задаче обязательно отмечать, **кем** она сделана —
исполнителем (`forge-executor`) или самим блок-агентом.

**Статус:** T176 закрыта, T177–T179 в работе.

### T176 — токены как статика продукта · сам · закрыта

Сделал сам: задача про значения дизайн-системы, и решать, что считать «не
потерянным», исполнителю нельзя — на этом строится вся остальная работа блока.

Что сделано:

- `src/web/static/web/tokens.css` — копия эталонного `tokens.css`. Значения не
  тронуты: 115 переменных в `:root`, 61 переопределение в `[data-theme="dark"]`
  (пересчитано разбором обоих файлов, совпадает). Контракт называл 165 — это
  число ни с чем не сходится: 115 имён, 176 объявлений вместе с тёмной темой.
  В журнал, не в `tasks.md`: правки в общие файлы — за диспетчером.
- Путь `static/web/`, а не `static/pnl/` из эталона: статика в проекте разложена
  по именам приложений Django (`timesheets/…`), и одно приложение под двумя
  именами искали бы дольше. Отличие записано в шапке самого файла.
- Шрифты локально: `static/web/fonts/`, 12 файлов `.woff2`, 180 КБ — Golos Text
  (переменный, 400–700) и IBM Plex Mono 400/500/600, подмножества latin,
  latin-ext, cyrillic. Это шаг 2 раздела «Как перенести в Django»: в прототипах
  шрифты приходят ссылкой на Google Fonts, в проде внешние загрузки запрещены.
  Блок `@font-face` прописан в начале `tokens.css`, как и сказано в эталоне;
  пути относительные, потому что `{% static %}` внутри CSS не работает.
- Подключено в `base.html` **до** `{% block head %}`: правила читают `var(--…)`,
  и лист, приехавший раньше значений, рисовал бы страницу без них.
- Причина, по которой оформление жило инлайном («runserver при DEBUG=0 статику
  не отдаёт»), больше не действует: статику отдаёт whitenoise (issue #68).
  Проверено фактически, не по коду: `tests/test_static_files.py` берёт файл при
  `DEBUG=False` — 200 и непустое тело.

Проверено: `tests/test_visual_tokens.py` (2 новых теста — значения доехали,
шрифты локальные и на диске), `tests/test_static_files.py` расширен тремя
файлами (токены и два шрифта) — 9 зелёных; выборка веб-тестов (i18n, страницы
отказа, онбординг) — 46 зелёных.

### T177 — стили из `base.html` в файл и на токены · сам · закрыта

Сделал сам, не исполнителю. Это не механическая подстановка: у продукта свои
имена классов, а у эталона свои, и на каждое правило приходилось решать, какой
токен несёт тот же смысл (`--muted` → `--ink-2`, но `--warn` разошёлся на
`--warn` для рамок и `--warn-ink` для текста). Такое решение исполнителю не
передать — контракта, который его снимает, не существует.

Что сделано:

- `src/web/static/web/app.css` — весь лист из `base.html`. Перенесены **все**
  156 селекторов; сверено разбором обоих файлов, не глазами. Не перенесены
  два: `:root` (уехал в `tokens.css`) и `button:focus-visible, a:focus-visible`
  — заменён общим `:focus-visible` дизайн-системы, который шире: фокус теперь
  виден на всём, чем можно управлять с клавиатуры, а не только на кнопках и
  ссылках (восьмой принцип эталона).
- В `base.html` блока `<style>` нет. Литеральных цветов в шаблонах
  `src/web/templates/` — ни одного (проверено `tests/test_visual_tokens.py`,
  46 файлов параметром).
- Комментарии листа сохранены дословно: в них записаны дефекты, купленные
  болью (отрицательный отступ, наложивший текст на кнопки; `display: none`,
  выкинувший подсказку из дерева доступности; пунктир у выгрузки).
- **Потребитель общего модуля тоже переехал:** `src/timesheets/.../grid.css`
  читал `--muted`, `--warn-bg`, `--bg` — имён, которых в новом наборе нет.
  Оставить как было значило бы отдать экран табеля без цветов; переименовано и
  переведено на токены целиком, литералов там тоже больше нет.
- Каркас: колонка содержимого 1360, а не 1280 из эталона, и `min-width` у
  `body` не поставлен. Причины в самом файле: ведомость обязана читаться на
  1440 (измеренное требование), а фиксированная ширина `body` отменила бы
  инвариант «прокручивается контейнер, а не страница» и уронила бы 768.

**Три теста были красными, и ни один не от порчи оформления. Разбирал каждый.**

1. `test_field_help_is_reachable` искал правило показа подсказки **в разметке
   страницы** — потому что лист был инлайновым. Проверка та же, источник
   другой: правило читается из файла статики, и рядом добавлена проверка, что
   страница этот файл просит (правило в неподключённом листе не действует).
2. `test_readme_numbers` — в README число тестов. Обновлено: 1594 и 8.
3. `test_web.py::test_period_page_opens_on_seed_data` — **фиктивная зелень,
   прожившая до сегодня.** Проверка утверждала «сотрудники сида должны
   считаться» и делала `assert "32" in text`. Зеленела она на «32» из
   `padding: 14px 32px` в инлайновом листе: сотрудников в сиде **35**, и
   числа 32 на странице нет вовсе. То есть проверка не проверяла ничего два
   месяца и покраснела только потому, что лист уехал в статику. Заменена на
   разбор пары «подпись — значение»: `Сотрудников в табеле → 35`.
   **Находка для беклога** (сам issue не завожу — общие файлы за диспетчером):
   такой же риск у любой проверки, ищущей число по странице целиком.

Проверено фактически: полный прогон `1594 passed, 8 skipped` (было 1545 и 8;
новых пропусков ноль). Линтер `ruff check src/ tests/` чист.

### T178 — каркас и шапка по эталону · сам · закрыта

Сделал сам. Задача решает расхождение с эталоном (сколько ролей показывать) и
переносит инварианты, купленные дефектами; ни то, ни другое исполнителю не
передать контрактом.

Предыдущий заход блока встал по watchdog посреди этой задачи. Уцелевшее (`90cc57c`)
я проверил и принял: разметка шапки, компонент пункта навигации, фильтр
`in_section` и правила листа были на месте и целые. Не было **приёмки** — ни одной
проверки на шапку в прогоне не существовало вовсе, и это главное, что сделано здесь.

Что сделано:

- `src/web/templates/web/components/appnav_item.html` — пункт навигации одним
  компонентом вместо шести копий. Текущий раздел — `span[aria-current="page"]`,
  а не ссылка: ссылка на открытую страницу обманывает и мышь (обещает переход,
  которого не будет), и диктор (перечисляет наравне с остальными, не сообщая,
  где человек). Тот же инвариант живёт у переключателя разреза ведомости.
- `in_section` в `templatetags/ui.py` — «человек в этом разделе» по **корню**
  адреса, с картой `NAV_BELONGS`: след расчёта, расчётный лист и табель остаются
  «Периодами», карточка платежа и инбокс — «Счетами». Сравнение адресов целиком
  гасило бы выделение на каждом вложенном экране, а пункт, потерявший выделение,
  читается как «вы ушли из раздела».
- Шапка по разделу 08: имя продукта (`Dodo` начертанием, `P&L` акцентом),
  разделы, распорка, «кто смотрит», роль, язык. Правила уехали в `app.css` на
  токены, ряд 50 пикселей — единственное число из эталона.
- **Роль — одна плашка** (решение владельца, #130 сам не решал). Пунктирной
  «временной» роли из эталона нет: она есть только у второго набора прав.
- Два осознанных отхода от эталона, оба записаны в самом листе: сильная строка —
  имя человека, а не название юрлица (один экран показывает разным людям разные
  числа, и человек должен видеть, чьими глазами смотрит); кружка с инициалами
  нет — при показанном имени это второй способ сказать то же самое.
- Узкий экран: вместо `min-width` у body (эталонный приём) шапка переносится на
  две строки распоркой. Причина в листе: фиксированная ширина body отменила бы
  инвариант «прокручивается контейнер, а не страница» и уронила бы требование
  «работает на 768» — планшет получил бы горизонтальную прокрутку всего вместе
  с шапкой.

**Приёмка — `tests/test_appbar.py`, 25 проверок, все новые.** Проверяется на
настоящих страницах, а не только на фильтре: карта `NAV_BELONGS` может быть
верной, а шапка — собранной мимо неё. Что закреплено: текущий раздел не ссылка;
выделение держится на вложенном экране (`/payments/new/` → «Счета»); выделен
ровно один раздел; плашка роли одна у каждой из четырёх ролей; партнёр, регистры
и точка на экране; точка **не** названа тому, кто ею не ограничен; разделы,
которых роль не ведёт, отсутствуют, и обратная сторона — администратор их видит;
страница просит оба листа; фокус виден на всём, чем можно управлять с клавиатуры
(а не только на кнопках, как было до T177); на планшете шапка переносится и у
body нет фиксированной ширины; на телефоне нажимаемое не меньше `--tap-min`.

Находка по ходу, для беклога (issue сам не завожу — общие файлы за диспетчером):
`principal.role_title` склеивает роли через `" + "`, если у человека их несколько
в базе. Плашка при этом одна, то есть решение владельца разметкой соблюдено, но
текст внутри неё может назвать два набора прав. Разграничение ролей ведёт блок
`stands` — трогать не стал.
