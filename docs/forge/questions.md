<!-- managed by forge: формат строк ниже не менять — файл парсят forge-build и forge-status -->
# Вопросы владельцу

<!-- id: Qnnn (Q001, Q002, ...). статус: open (ждёт ответа) | answered (ответ в колонке «ответ») | auto (решено автономно диспетчером; колонка «ответ» содержит ссылку вида «→ Dnnn» на decisions.md). "блокирует" — id задач из tasks.md через запятую или «—». -->

| id | статус | блокирует | вопрос | ответ |
|---|---|---|---|---|
| Q001 | open | — | Доплата до минимума считается до минимальной цены труда по стране или до личной ставки сотрудника? В июне 2026 различить нельзя: единственный случай пришёлся на человека с коэффициентом 1,0, где оба варианта дают одно число. Вопрос бухгалтеру Сербии | — |
| Q002 | open | — | Какие ещё ситуации, кроме больничного, вызывают доплату до минимума? | — |
| Q003 | open | — | Топли оброк считается по рабочим дням или по отработанным часам? В таблице бухгалтерии два несовместимых подхода на разных листах: `pola radnog vremena puno` считает по дням (88 часов на полставке = все 22 дня = полные 1500), `pola radnog vremena` — по часам (20 часов = 170,45) | — |
| Q004 | open | — | Полный перечень процентов оплаты по типам отсутствия: известны отпуск 100% и больничный 65%. Что с первыми днями больничного, уходом за ребёнком, травмой на работе? | — |
| Q005 | open | — | По какому признаку сотрудник попадает в схему с минимальной базой для взносов, а не в обычную полставку? | — |
| Q006 | answered | — | Ночные часы: отдельная ставка или зашиты в коэффициент? В июне не встречались, в пресете стоят заготовкой с пометкой `unverified` | Владелец 26.08.2026 дословно: «надбавки это коэффициенты за вечерние часы, ночные часы, выходные и прочее». То есть коэффициент к типу часа, а не отдельная ставка и не множитель у человека → D054 |
| Q007 | open | — | Топли оброк как компенсация питания — это списанный продукт. Возможен двойной учёт: сумма попадает в зарплату, а сам продукт — в статью «Питание персонала». Как эти две цифры соотносятся в P&L? | — |
| Q008 | open | — | Переводы и пополнения наличных заведены статьями с видом `transfer` и в P&L не попадают. Нужен ли по ним отдельный отчёт движения наличных? Вопрос из работы над таблицей фактов | — |
| Q009 | open | — | Статья для налогов с зарплаты сейчас ищется по коду `payroll_taxes` параметром функции проводки. Если у партнёра код другой — делать настройкой тенанта, а не аргументом? Вопрос из работы над таблицей фактов | — |
| Q010 | answered | — | Что видит управляющий точки: спека даёт ему только официальный регистр, а сид — официальный и дополнительный. Расхождение нашёл блок auth; данные не трогали, потому что на числах сида снята приёмка второй очереди. Рекомендация: оставить как в сиде (официальный + дополнительный) — управляющий видит надбавки своей смены, иначе он не сможет проверить собственную точку; тогда правится спека | Владелец 2026-08-07: видит официальный и дополнительный. Сид был прав, спека приведена к ответу (→ D031) |
| Q011 | answered | — | Чем меряется работа курьера: сейчас в пресете у группы `couriers` схема расчёта, где бруто равно нето и взносов нет, а часы для них условны. Нужно понять, платят им за часы, за доставки или фиксированную сумму — от этого зависит, что вводить в табель. Вопрос бухгалтеру Сербии (issue #47) | Владелец 2026-08-07: надо предусмотреть оба варианта — и часы, и сдельную работу (→ D032, задача T075) |
| Q012 | answered | T087 | Бухгалтер по спеке должен считать период («вводит часы, считает, утверждает» — must), но фактически не может НИКОГДА: расчёт пишет строки во все регистры, а роль, которая какой-то из них не видит, не может его записать — база отвергает `insert ... returning` (политика `select` применяется к возвращаемым строкам). В любом реальном месяце есть компоненты дополнительного регистра, поэтому считать выходит только у директора. Развилку нельзя закрыть умолчанием, потому что оба выхода меняют обещанное: (а) разрешить расчёт роли, не видящей всех регистров — тогда она пишет то, чего не увидит, и это надо признать сознательно; (б) признать, что считает только тот, кто видит всё, и поправить спеку и роли. Рекомендация: вариант (б) — он не ослабляет разграничение доступа, а расчёт и так запускается раз в месяц одним человеком. Тогда у бухгалтера остаются ввод часов, утверждение, сверка и выгрузки | → D036: у партнёра бухгалтер и оперативный директор равны, бухгалтеру открываются все регистры; сужение доступов отложено |
| Q013 | answered | — | Есть ли у расхода из кассы регистр учёта, как у зарплаты? Вариант А: расход всегда официальный, а «серые» траты просто не вносятся. Вариант Б: регистр выбирается при внесении, как у сотрудника. Рекомендация — Б: механизм уже есть и ничего не стоит, а вариант А потом не расширить без переписывания всех внесённых строк | Владелец 2026-08-14: «Из кассы берем только официально. Но есть чёрная касса, где по дефолту идёт в чёрную. Не понимаю вопроса». То есть регистр расхода следует из того, ИЗ КАКОЙ КАССЫ платили, а не выбирается к каждой трате: официальная касса → официальный регистр, чёрная → внутренний по умолчанию. Вопрос был задан неудачно — про свойство операции, а не про источник денег (→ D039) |
| Q014 | answered | — | Нужен ли остаток по кассе точки (сколько наличных должно лежать) или достаточно списка расходов? Остаток требует ещё и прихода — выручки наличными, а она придёт только с коннектором Dodo IS (этап 6). Рекомендация: пока список и итог за месяц, остаток — когда появится приход | Владелец 2026-08-14: «Кассу не трогаем нигде же? У нас сервис по сборке ПНЛ, нам тут касса не важна». Остаток по кассе не ведём — ни сейчас, ни потом (→ D040) |
| Q015 | answered | — | Какой список статей расходов взять за основу? У бухгалтера Сербии он наверняка уже есть в её таблице. Рекомендация: завести справочник пустым и наполнить с её файла, а не выдумывать — иначе одинаковые траты назовутся по-разному, и это вскроется при первой сборке P&L | Владелец 2026-08-14: «Берём за основу её справочник, но должна быть возможность редактировать и дополнять» (→ D041) |
| Q016 | answered | — | Нужен ли НДС отдельным полем у расхода? В Сербии ПДВ 20%, и если партнёр его зачитывает, в P&L должна попадать сумма без налога. Рекомендация: хранить сумму документа и ставку отдельно (поля в схеме фактов есть), а показывать ту, которую попросит бухгалтер | Владелец 2026-08-14: «НДС нужен, да. Это вообще важная вещь в Сербии и вообще. Хотя вообще в итоговом ПНЛ мы обычно не показываем НДС». Хранить обязательно, в P&L по умолчанию показывать без НДС (→ D042) |
| Q017 | open | T151 | Считаем ли обязательства — неоплаченные счета — или в P&L попадает только оплаченное? Рекомендация: хранить оба события (счёт и платёж) с разными датами, а в P&L по умолчанию брать период учёта счёта: так отчёт не скачет от того, когда именно ушли деньги. Неоплаченное тогда видно отдельным числом, и его можно не показывать, если не нужно | — |
| Q018 | open | — | Нужен ли учёт авансов и предоплат поставщику, и как относить оплату, покрывающую несколько счетов? Рекомендация: в первой версии одна оплата — один счёт (или оплата без счёта), а частичные и общие платежи отложить до первого настоящего случая у партнёра | — |
| Q019 | open | T151 | Нужен ли отдельный регистр для трат мимо официального контура? Та же развилка, что решена для кассы в D039, и ответ вероятно тот же: регистр следует из ИСТОЧНИКА денег (с какого счёта или из какой кассы платили), а не выбирается к каждой трате. Рекомендация: завести источник платежа справочником, как кассу | — |
| Q020 | answered | — | Доступ к Dodo IS API: упирается ли он в публикацию приложения в маркетплейсе и модерацию? | Снят собственной проверкой, а не ответом владельца: раздел «Начало работы» на docs.dodois.io говорит другое — приложение заводится в личном кабинете разработчика (marketplace.dodois.io/manage/apps), оттуда Client Id и Secret, дальше OAuth и вызовы под учёткой Dodo IS. Публикация в маркетплейсе — про РАСПРОСТРАНЕНИЕ другим франчайзи, и её начальное условие дословно «у вас уже есть приложение, которое использует API». Шестая очередь ничем не заблокирована (→ D043) |
| Q021 | answered | — | **CI мёртв: Actions не запускаются по биллингу после перевода репозитория в приватный, а защита ветки на бесплатном тарифе для приватного репозитория недоступна (403 Upgrade to Pro).** Прогон 32449656102: все три задания отказали за 2 секунды с «recent account payments have failed or your spending limit needs to be increased». В интерфейсе это выглядит как красный прогон, то есть как будто тесты упали. Прогон проекта тяжёлый — 10–12 минут, триггер на каждый пуш и каждый PR. Варианты: поднять лимит расходов или Pro (работает всё, включая защиту ветки) / вернуть репозиторий в публичные (Actions бесплатны, но отменяет решение о приватности от 20.08) / урезать CI до PR-только без сборки образа / прогонять локально pre-push хуком. Рекомендация — поднять лимит расходов. Разбор — issue #137. **Отправлен 2026-08-21 07:50** | Владелец 2026-08-22: «терпим до сентября и там всё будет окей». CI до сентября не чиним, денег не несём, проверки гоняются локально перед слиянием; к тарифу возвращаемся в сентябре (→ D050) |
| Q022 | open | — | **Демо решено переключателем роли, но есть второй возможный ответ, и он меняет продукт, а не демо.** T163 закрыта так: в демо на месте раздела, которого роль не ведёт, стоит предложение «этот раздел ведёт такая-то роль, открыть ею». Права при этом не меняются ни на копейку. Альтернатива — **выдать оперативному директору ведение справочников**: тогда он видит их и в продукте, и в демо без переключения. Это решение про разделение обязанностей у партнёра, а не про демо, поэтому принято не автономно. Рекомендация: оставить как сделано — директор отвечает за сборку P&L, а справочники и правила ведёт администратор сети (D047), и размывать это ради удобства показа не стоит. Записано также в докстринге `src/demo/switching.py`. **Отправлен 2026-08-21 10:15** | — |
| Q023 | open | — | **Итоги ведомости видит только тот, кому видны ВСЕ регистры — значит бухгалтеру с урезанным набором вместо налогов и взносов показываются прочерки.** Вскрылось на первом читателе `payslip_totals` в продукте (экран истории выплат по человеку, T166): политика там жёстче, чем предполагалось — право на итоги даёт `app_sees_every_ledger` (T071), а не видимость конкретной строки. Сегодня это никого не задевает, потому что у партнёра бухгалтер и оперативный директор видят все три регистра (D036). Но как только доступы начнут сужаться — а механизм для этого держится живым нарочно — такая роль увидит начисления и не увидит налогов. Лечится не экраном, а раскладкой итогов по регистрам, и там упирается в предметный вопрос, заведённый ещё в миграции `0023`: **как делить взносы, посчитанные от общего нето?** Ответа у партнёра нет. Рекомендация: пока оставить как есть (прочерк с названной причиной на экране честнее выдуманного числа), а вопрос про раскладку задать бухгалтеру вместе с остальными по зарплате. **Отправлен 2026-08-22** | — |
| Q024 | open | — | Какие надбавки за вечерние часы и за работу в выходные у партнёра и с какой даты? Закон Сербии их не требует (в отличие от ночных +26% и праздника), значит величина берётся из коллективного договора или внутренней политики сети. В правилах страны заведены типы часов с нейтральным коэффициентом 1,00 (D054) — партнёр ставит своё значение на экране правил. Выдумывать процент за него нельзя: он молча уехал бы в расчёт | — |
