"""Управляющий точки видит только своих людей (T057).

**Что было.** Видимость точек (миграция `0011`) закрыла `units`, `timesheets`,
`employment_terms`, `payslips` и `pay_components` — и обошла `employees`. Там же
это записано как осознанный пропуск: «точка у сотрудника не хранится, она в
условиях найма, закрывать пришлось бы присоединением». Присоединение и написано
здесь.

Цена пропуска выше, чем казалась: `employees.external_id` — сквозной ключ
человека, в Сербии это JMBG, национальный идентификатор. Справочник отдавал его
по всей сети любому управляющему. На сегодняшних экранах этого не видно (список
строится от табеля), но спека обещает «видит только своих людей» как гарантию
базы, а не как свойство того, что ни один экран пока не спросил лишнего.

**Что считается «своим человеком».** У сотрудника нет колонки с точкой: точка
живёт в условиях найма, а те версионируются по датам. Правило выбрано такое:

    свой — тот, у кого есть хотя бы одна версия условий найма на видимой точке,
    независимо от дат версии.

Почему без дат:

* *уволенный* остаётся виден своей точке. Его версия закрыта `valid_to`, но
  ведомость и табель закрытого периода никуда не делись — а без имени они
  нечитаемы. Право видеть человека даёт факт работы у этой точки, а не то, что
  он работает сегодня;
* *переведённый в середине месяца* виден обеим точкам: обе версии настоящие.
  Сумм это не открывает — ведомость приходит одной строкой с одной точкой
  (`payslips_payrun_employee_uniq`), и её режет политика из `0011`;
* дата «сегодня» в политике вообще не годится: она сделала бы видимость
  зависящей от момента запроса, и один и тот же закрытый период читался бы
  по-разному в июне и в июле.

**Человек без условий найма виден только тем, у кого нет ограничения по
точкам.** Это отличается от правила `0011` для строки с пустым `unit_id`, и
отличается сознательно: там точка была и пропала (`on delete set null`), а здесь
её не назначали ни разу. Показать управляющему людей, которых к его точке никто
не приписывал, — ровно та утечка справочника, ради которой заведена задача.
Следствие для записи: управляющий не может завести сотрудника (у нового строки
условий найма ещё нет). Заводит их администратор сети, у которого ограничения по
точкам нет; отдельного права на это сегодня всё равно не проверяется нигде,
кроме `directory.manage` (T064).

**Почему функция не `security definer`.** Проверено на живой базе: `security
definer` **не** обходит `force row level security`, если владелец функции —
обычная роль, а не суперпользователь. То есть definer дал бы разное поведение на
конфигурации разработки (владелец-суперпользователь) и на управляемом Postgres
(T052) — и на второй, читая условия найма «как владелец», функция всё равно
видела бы их через политики. Здесь этого и не нужно: подзапрос идёт под
политиками `employment_terms`, а условие видимости точки написано явно, чтобы
правило читалось из кода, а не выводилось из чужой политики.
"""
from django.db import migrations

FUNCTIONS = """
-- Правило «свой человек» в одном месте. Условие намеренно не повторяется в
-- политике: разъехавшиеся копии одного правила и есть способ, которым доступ
-- ломается незаметно.
create or replace function app_employee_is_visible(p_tenant uuid, p_employee uuid)
returns boolean
language sql stable
as $$
    select app_unit_ids(p_tenant) is null   -- ограничения по точкам нет — виден каждый
        or exists (
            select 1
              from employment_terms t
             where t.employee_id = p_employee
               and t.tenant_id = p_tenant
               -- Даты версии не проверяются: см. шапку миграции. Уволенный и
               -- переведённый остаются своими для точки, где работали.
               and app_unit_is_visible(t.tenant_id, t.unit_id)
        )
$$;

comment on function app_employee_is_visible(uuid, uuid) is
    'Свой ли это человек: есть ли у него условия найма на видимой точке. Даты версий не учитываются';
"""

DROP_FUNCTIONS = "drop function if exists app_employee_is_visible(uuid, uuid);"

# `for all`, а не только `for select`: иначе чужого человека нельзя было бы
# прочитать, но можно было бы переименовать вслепую.
POLICY = """
create policy unit_visibility on employees
    as restrictive for all
    using (app_employee_is_visible(tenant_id, id))
    with check (app_employee_is_visible(tenant_id, id));
"""

DROP_POLICY = "drop policy if exists unit_visibility on employees;"


class Migration(migrations.Migration):
    dependencies = [("core", "0012_timesheet_days")]

    operations = [
        migrations.RunSQL(FUNCTIONS, DROP_FUNCTIONS),
        migrations.RunSQL(POLICY, DROP_POLICY),
    ]
