"""Подневные часы закрываются по точке так же, как месячные (T022).

**Что было.** T044 поставила разграничение по точкам на `timesheets`, но
`timesheet_days` появилась в другой копии репозитория тем же днём и в список не
попала. Своей точки у дня нет — она у строки-родителя, — поэтому условие
`unit_id` там просто не к чему было применить, и таблица осталась закрытой
только по тенанту. Дыра была названа в журнале блока сразу и подпёрта тестом,
который утверждал её как факт; здесь он падает и переписан.

Цена дыры не теоретическая: месячный итог `timesheets.hours` собирается **из
дней** (`timesheets.store`), поэтому чтение и запись дня — это чтение и запись
тех же часов, только другой таблицей. Управляющий, спросивший `timesheet_days`
напрямую, получал подневные часы всех точек партнёра.

**Почему через родителя, а не колонкой точки в самой таблице.** Точка на строке
табеля стоит по решению T019: человек на двух точках — это две строки, и день
принадлежит одной из них. Продублировать `unit_id` ещё и на дне значило бы
завести два места для одного факта — они разъезжаются молча, и тогда непонятно,
какое из них решает доступ. Тот же приём уже применён к `pay_components`
(миграция `0011`): у компонента выплаты своей точки тоже нет.

**Почему `as restrictive` и `for all`.** Пермиссивные политики Postgres
объединяет через OR — «точка видна» не сужало бы выборку вообще. `for all`, а не
`for select`: иначе управляющий не видел бы чужие дни, но мог бы их **писать**.
"""
from django.db import migrations

POLICY = """
-- Родителя не видно — не видно и дня. Условие видимости самой строки табеля
-- живёт в одном месте (`app_unit_is_visible`, миграция 0011) и здесь не
-- повторяется: разъехавшиеся копии одного правила и есть тот способ, которым
-- доступ ломается незаметно.
create policy unit_visibility on timesheet_days
    as restrictive for all
    using (exists (select 1 from timesheets t where t.id = timesheet_days.timesheet_id))
    with check (exists (select 1 from timesheets t where t.id = timesheet_days.timesheet_id));
"""

DROP_POLICY = "drop policy if exists unit_visibility on timesheet_days;"


class Migration(migrations.Migration):
    # Нумерация с 0030: третья очередь строится параллельно несколькими блоками
    # в разных копиях репозитория, номера разведены заранее (`0030+` —
    # timesheets, `0040+` — payrun). Два листа в графе миграций Django ломают
    # слияние, поэтому зависимость от последней миграции соседа — порядковая,
    # а не смысловая.
    dependencies = [("core", "0041_payrun_lifecycle")]

    operations = [migrations.RunSQL(POLICY, DROP_POLICY)]
