"""Статус «открыт заново» у расчёта периода (T023).

**Почему отдельной миграцией и почему номер 0040.** Postgres не разрешает
пользоваться новым значением enum в той же транзакции, где оно добавлено, а
Django накатывает каждую миграцию транзакцией. Всё остальное устройство цикла
(функции, триггеры, журнал) на `reopened` ссылается, поэтому добавление значения
обязано быть отдельным шагом и до них.

Номер начинается с 0040 не по порядку: третья очередь строится параллельно
несколькими блоками в разных копиях репозитория, и номера разведены заранее —
`0030+` заняты блоком `timesheets`, `0040+` наши. Два листа в графе миграций
Django ломают слияние, и чинить это после слияния дороже, чем разойтись
номерами до него.

**Почему обратный ход пустой.** Значение enum в Postgres удалить нельзя
(`alter type ... drop value` не существует). Откат оставляет значение в типе, и
это безвредно: без функций и триггеров из следующей миграции никто в него не
переводит. Молчать об этом нельзя — отсюда и комментарий.
"""
from django.db import migrations

FORWARD = """
-- Явный результат отката, а не молчаливый возврат в 'draft': по состоянию
-- расчёта должно быть видно, что период открывали.
alter type payrun_status add value if not exists 'reopened' after 'approved';
"""


class Migration(migrations.Migration):
    # Добавление значения enum держим вне транзакции: так оно точно доступно
    # следующей миграции, независимо от версии Postgres.
    atomic = False

    dependencies = [("core", "0023_totals_by_role")]

    operations = [migrations.RunSQL(FORWARD, migrations.RunSQL.noop)]
