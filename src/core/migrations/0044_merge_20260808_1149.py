"""Сводит две ветки миграций в одну. Своих изменений не вносит.

Откуда взялись две ветки. Блоки `timesheets` и `payrun` строились параллельно в
разных копиях репозитория, и обоим было велено разойтись по номерам: `0030+`
одному, `0040+` другому. Номера действительно не столкнулись — а граф всё равно
разошёлся: обе цепочки выросли от общего родителя `0023`, и после слияния веток
у Django оказалось два листа (`0031_timesheet_closing` и
`0043_payrun_journal_cascade`). Контейнер приложения на старте отказался
накатывать миграции вовсе.

Вывод на будущее, чтобы не повторять: разведённые номера защищают только от
столкновения имён файлов. От двух листьев защищает **зависимость**: та цепочка,
что вливается второй, должна объявить зависимость от листа первой — либо, как
здесь, ветки сводятся пустой миграцией слияния при приёмке.

Пустая она не по недосмотру: обе ветки меняли разные таблицы (закрытие часов по
точкам и журнал переходов расчёта), пересечения между ними нет, сводить нечего —
нужен только общий лист.
"""
from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('core', '0031_timesheet_closing'),
        ('core', '0043_payrun_journal_cascade'),
    ]

    operations = [
    ]
