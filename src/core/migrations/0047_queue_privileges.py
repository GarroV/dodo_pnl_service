"""Права роли приложения на таблицы очереди — ровно те, что нужны (T024).

**Что было не так по умолчанию.** Миграция `0005_app_role` выдаёт `app_user`
чтение и запись на все будущие таблицы схемы (`alter default privileges`) — это
правильно для данных продукта, они закрыты политиками. Таблицы `django_q`
политик не имеют и иметь не могут: тенанта в них нет, а очередь общая. То есть
после установки очереди роль приложения читала и правила её целиком.

Что там лежит и почему это всё-таки не пустяк: полезная нагрузка задачи —
подписанный `SECRET_KEY` pickle с именем функции и идентификаторами. Сумм и ФИО
в ней нет, подделать её без ключа нельзя, но по очереди было видно, у кого из
партнёров сейчас считается период, а `django_q_task` хранит ещё и результаты
задач всех тенантов разом. Продукт, у которого изоляция держится на политиках
базы, не должен иметь рядом таблицу, куда любой вошедший может писать.

**Что оставлено.** Задачу ставит веб-запрос, а он идёт ролью `app_user` внутри
транзакции запроса — и это осознанно: очередь на самом Postgres тем и хороша, что
задача становится видимой рабочему процессу ровно в момент коммита запроса,
который её поставил. Значит, роли нужны:

- `insert` в `django_q_ormq` — поставить задачу;
- `select` **только на колонку `id`** — Django получает первичный ключ через
  `RETURNING`, и без права на колонку вставка упала бы. Полезная нагрузка при
  этом роли уже не видна;
- права на последовательность ключа.

Всё остальное — забирается. Рабочий процесс очереди ходит ролью подключения
(владельцем), политик и этих ограничений на нём нет: он и должен читать очередь
целиком, у него нет пользователя, от чьего имени можно было бы сузить выборку.
Контекст пользователя он выставляет уже внутри задачи, для работы с данными.

Зависимость пришпилена к последней миграции `django_q`: раньше её таблиц не
существует, и `revoke` было бы не на что выдавать. Если очередь однажды добавит
свои миграции поверх, права на новые таблицы придётся выдать заново — на это
есть тест `test_the_queue_tables_are_closed_to_the_app_role`.
"""
from django.db import migrations

TABLES = ("django_q_ormq", "django_q_task", "django_q_schedule")

FORWARD = f"""
revoke all on {", ".join(TABLES)} from app_user;

grant insert on django_q_ormq to app_user;
-- Только id: Django берёт первичный ключ через RETURNING, а payload роли
-- приложения видеть незачем.
grant select (id) on django_q_ormq to app_user;
grant usage, select on sequence django_q_ormq_id_seq to app_user;
"""

BACKWARD = f"""
grant select, insert, update, delete on {", ".join(TABLES)} to app_user;
"""


class Migration(migrations.Migration):
    dependencies = [
        ("core", "0046_payrun_jobs"),
        # Таблицы очереди должны уже существовать: иначе revoke не на что выдавать.
        ("django_q", "0019_alter_task_options_alter_ormq_key_alter_ormq_lock_and_more"),
    ]

    operations = [migrations.RunSQL(FORWARD, BACKWARD)]
