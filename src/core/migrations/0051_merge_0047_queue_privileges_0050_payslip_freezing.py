"""Сводит две ветки миграций в одну. Своих изменений не вносит.

Второй такой файл за прогон, и причина у него другая — её стоит записать, иначе
третий появится по тому же кругу.

В первый раз (`0044_merge`) ветки разошлись потому, что блокам развели только
**номера** миграций. Вывод сделали: мало номера, нужна **зависимость**. И оба
следующих блока — фоновый расчёт (`0046`, `0047`) и построчная заморозка
(`0050`) — честно объявили зависимость от тогдашнего листа `0044_merge`. Граф
всё равно разошёлся: зависимость от **общего** листа делает ветки братьями, а
не цепочкой. Два ребёнка одного родителя — это ровно два листа.

Правильная формулировка, которой не было ни в первый, ни во второй раз: вторая
вливаемая ветка должна зависеть от листа **уже влитой ветки**, а не от того
листа, который оба видели на старте. Знать это заранее блок не может — он не
знает, кого примут первым. Значит сведение веток принципиально работа
диспетчера при приёмке, а не агента при разработке; агенту достаточно не
столкнуться по именам файлов.

Пустая она не по недосмотру: очередь и заморозка меняли разные таблицы,
пересечения нет — нужен только общий лист.
"""
from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('core', '0047_queue_privileges'),
        ('core', '0050_payslip_freezing'),
    ]

    operations = [
    ]
