"""Сдельная величина в табеле: доставки или фиксированная сумма (T075, D032).

**Зачем колонка.** Работу курьера меряют не обязательно часами. Чем именно — за
доставки, фиксированной суммой или всё-таки за часы, — бухгалтер Сербии ещё не
ответил (Q011), и владелец решил поддержать оба пути сразу (D032). Способ выбран
правилом `groups.<код>.work_measure`, а вводимая величина должна где-то лежать:
в `hours` её положить нельзя — там ключами идут типы часов страны, и «доставки»
среди них появиться не могут, иначе движок начислит за них по проценту от
часовой ставки.

**Почему одна колонка, а не две.** У группы мера ровно одна: либо доставки, либо
фиксированная сумма. Две колонки означали бы два ответа на один вопрос — и
однажды заполненными оказались бы обе, а какая из них считается, знал бы только
код. Что значит число, говорит правило, и оно же версионируется по датам.

**Почему не таблицей по дням**, как часы (D011). Смысл подневного хранения —
разрезать месяц по датам при переводе человека между точками. За месячным числом
доставок настоящих дат нет вовсе: ровная раскладка выдумала бы их, а разрезать
выдуманное так же бессмысленно, как не разрезать. Когда доставки приедут из Dodo
IS, они приедут вместе со своими датами — и тогда это будет своя таблица со
своим источником, а не догадка.

**Обратимость.** Обычное добавление колонки со значением по умолчанию: старый
код её не читает, откат сносит только её. Ни одной существующей строки правка не
трогает — у всех ноль, и расчёт по часам остаётся прежним до копейки.
"""
from django.db import migrations, models

COMMENT = """
comment on column timesheets.piece_value is
    'Сдельная величина за месяц (D032): число единиц либо сумма — что именно, задаёт правило work_measure у группы сотрудника. У почасовой группы не читается';
"""

DROP_COMMENT = "comment on column timesheets.piece_value is null;"


class Migration(migrations.Migration):
    # Номер с 0140: параллельные блоки строятся в разных копиях репозитория, и
    # диапазоны номеров разведены заранее. Зависимость от текущего листа master
    # порядковая, а не смысловая: два листа в графе миграций Django ломают
    # слияние.
    dependencies = [("core", "0110_director_closes_units")]

    operations = [
        migrations.AddField(
            model_name="timesheet",
            name="piece_value",
            field=models.DecimalField(db_default=0, decimal_places=2, max_digits=14),
        ),
        migrations.RunSQL(COMMENT, DROP_COMMENT),
    ]
