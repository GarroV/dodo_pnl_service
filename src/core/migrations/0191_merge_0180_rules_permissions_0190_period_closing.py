"""Сводит две ветки миграций в одну. Своих изменений не вносит.

Третий такой файл за прогон, и причина всё та же: два блока строились
параллельно от общего листа, номера им развели заранее (`0180` и `0190`), а
номера от расхождения графа не спасают — от него спасает зависимость, и объявить
её на старте нечем, потому что блок не знает, кого примут первым.

Вывод, записанный после второго случая, подтвердился: сведение веток — работа
диспетчера при приёмке, а не агента при разработке. Агенту достаточно не
столкнуться по именам файлов.

Пустая она не по недосмотру: `0180` добавляет политики на переопределения правил,
`0190` — закрытие учётного месяца вместе с утверждением расчёта. Разные таблицы,
пересечения нет — нужен только общий лист.
"""
from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('core', '0180_rules_permissions'),
        ('core', '0190_period_closing'),
    ]

    operations = [
    ]
