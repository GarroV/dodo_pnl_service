"""Морозить строку вправе тот, кому отдан весь её расчёт (T101).

**Что течёт.** У `payslip_freezes` было три условия — тенант, видимость строки
ведомости и право `payslip.freeze`, — и ни одного про регистры учёта. А у самой
`payslips` политики `ledger_visibility` нет вовсе (она есть у `pay_components`,
`payslip_totals`, `payslip_steps`): срез по регистру на этом маршруте держало
приложение, и там его забыли. Бухгалтер с одним официальным регистром замораживал
строку внутреннего: `POST /payslips/<id>/freeze/` отвечал `302 ?froze=1`, а
случайный uuid — `404`.

Следствий три, и они разного веса:

1. **Запись по строке, ни одного числа которой роли не показывают.** Заморозка
   означает «числа этой строки не меняются, и пересчёт её обходит» — то есть
   роль распоряжается деньгами регистра, которого не видит.
2. **Спорная строка исключается из переноса разницы** (T026). Отметка не
   декоративная: она меняет то, что доедет до следующего месяца.
3. **Пара «302 против 404» — оракул существования.** Ответ маршрута зависел от
   того, есть ли строка, а не от того, вправе ли роль её трогать.

**Правило — свойство роли, а не строки.** Морозить вправе тот, кому видны все
регистры учёта вообще: `app_sees_every_ledger(tenant_id)`, та же функция, на
которой с T071 стоит видимость итогов расчёта. Довод оттуда повторяется здесь
дословно: **пока разрешение зависит от содержимого строки, отсутствие
разрешения и есть сообщение**. Разреши бухгалтеру морозить строку, все регистры
которой ей видны, — и маршрут начнёт отвечать по-разному на официальную и на
внутреннюю строку, то есть отдаст ровно тот список, ради сокрытия которого всё
и делается. Состояний должно быть два: «морозит любую видимую строку» и «не
морозит ни одной».

Цена, которую надо знать: **бухгалтер перестаёт морозить строки вовсе**, включая
полностью официальные. Право `payslip.freeze` у неё остаётся — это два разных
условия, и второе не отменяет первое, — но экран кнопки больше не предлагает.
Так и должно быть по смыслу действия: заморозка относится к строке целиком, а не
к её официальной части, и «заморожу то, чего не вижу» — не разграничение.
Спорную строку морозит тот, кому отдан весь расчёт; месяц бухгалтер утверждает
по-прежнему — спорная строка его не держит, в этом вся T027.

**Почему только `with check`, без `using`.** Это не экономия, а обязательное
условие: сторож замороженной строки читает `payslip_freezes` функцией
`payslip_is_frozen()`. Она `security definer`, но `security definer` даёт права
владельца на объекты и **не отменяет `force row level security`**, если владелец
схемы — обычная роль (проверено в T065). Дай политике `using` — и на площадке
без суперпользователя функция ответила бы «не заморожено» всякому, кому заморозки
не видны: запрет снимался бы сам собой у того, кого он ограничивает. Тихо и
ровно в ту сторону, в которую ошибаться нельзя.

Что из этого следует и записано честно: сама строка заморозки роли по-прежнему
читается. Чисел в ней нет, регистра нет, а строка ведомости, на которую она
ссылается, и без того видна всем в тенанте (известный остаток, записанный в
контракте блока `db`). Закрыть чтение можно только вместе с ним — не отдельной
политикой поверх сторожа, а перестройкой того, как заморозка узнаётся.

**Форма политики — как в `0022` и `0050`.** `with check` без `using`: отказ
громкий (`new row violates row-level security policy`), а не тихое «изменено 0
строк», которое приложение приняло бы за успешную заморозку.
"""
from django.db import migrations

POLICIES = """
create policy payslip_freeze_whole_run_insert on payslip_freezes
    as restrictive for insert
    with check (app_sees_every_ledger(tenant_id));

create policy payslip_freeze_whole_run_update on payslip_freezes
    as restrictive for update
    with check (app_sees_every_ledger(tenant_id));

comment on table payslip_freezes is
    'Заморозка строки ведомости: по сотруднику идёт спор. Действующая заморозка — строка с пустым released_at. Ставит и снимает тот, кому отдан весь расчёт строки: все регистры учёта (T101)';
"""

DROP_POLICIES = """
drop policy if exists payslip_freeze_whole_run_update on payslip_freezes;
drop policy if exists payslip_freeze_whole_run_insert on payslip_freezes;

comment on table payslip_freezes is
    'Заморозка строки ведомости: по сотруднику идёт спор. Действующая заморозка — строка с пустым released_at';
"""


class Migration(migrations.Migration):

    dependencies = [
        ("core", "0191_merge_0180_rules_permissions_0190_period_closing"),
    ]

    operations = [
        migrations.RunSQL(POLICIES, DROP_POLICIES),
    ]
