"""Бухгалтеру открыты все три регистра, как директору (D036, T087).

**Что чинится.** Разграничение доступа строилось на догадке: раз регистр
`official` — это «то, что видит налоговая», значит бухгалтеру остальные регистры
показывать нельзя. У партнёра сегодня это не так. Ответ владельца на Q012
дословно: «И бухгалтер и операционный директор имеют равный доступ на данный
момент… А потом уже будет доступы закрывать там где надо».

Догадка была не безобидной — она ломала работу, которую спека требует прямо:

- **расчёт периода бухгалтером не проходил вовсе.** Расчёт пишет строки во все
  три регистра, а роли с неполным набором база запись отвергает (политики
  `payslip_ledgers`), — то есть «бухгалтер вводит часы, считает, утверждает»
  упиралось в отказ на второй трети пути;
- **выгрузка «Строки для P&L» приходила ей молча без налогов и взносов**:
  `payslip_totals` видны только роли со всеми регистрами.

**Что здесь меняется и что нет.** Меняется набор регистров ОДНОЙ роли. Механизм
видимости не трогается ни строкой: политики, `app_visible_ledgers()` и
`app_sees_every_ledger()` остаются на месте и работают — у управляющего точки
набор по-прежнему неполный (D031: официальный и дополнительный, без
внутреннего), и режет его та же самая политика. Сужать доступы будем позже, там
где надо, и механизм к тому времени должен быть жив.

**Почему по коду роли, а не по набору прав** — тот же довод, что в `0110`: код
роли единственный признак, который спека называет прямо. Партнёр, назвавший роль
иначе, правит её через экран ролей.

**Почему миграция, а не только сид.** Набор регистров — данные, и в базе, которая
уже живёт, сид больше не гоняется. Без миграции бухгалтер остался бы без расчёта
у всех, кто не пересоздаёт демо-данные.

**Почему в конце стоит проверка.** `roles` под `force row level security`: роль,
не обходящая политики, увидела бы отсюда ноль строк, `update` изменил бы ноль
строк молча, миграция «прошла» — а регистры не открылись (тот же корень, что у
issue #44). Поэтому проверяется результат, а не факт выполнения оператора.
"""
from django.db import migrations

GRANT = """
update roles
   set visible_ledgers = array['official', 'supplementary', 'internal']::ledger[]
 where code = 'accountant'
   and not (visible_ledgers @> array['official', 'supplementary', 'internal']::ledger[]);

do $$
declare
    stuck int;
begin
    select count(*) into stuck
      from roles
     where code = 'accountant'
       and not (visible_ledgers @> array['official','supplementary','internal']::ledger[]);
    if stuck > 0 then
        raise exception
            'регистры не доехали до % ролей бухгалтера: строки отрезаны политиками', stuck;
    end if;
end $$;
"""

# Откат возвращает роль к прежнему набору — одному официальному регистру. Это
# состояние до D036, а не «правильное»: откатывают миграцию, чтобы вернуться к
# коду той же версии, и набор обязан ехать вместе с ним.
REVOKE = """
update roles
   set visible_ledgers = array['official']::ledger[]
 where code = 'accountant';
"""


class Migration(migrations.Migration):

    dependencies = [
        ("core", "0210_freeze_whole_run"),
    ]

    operations = [
        migrations.RunSQL(GRANT, REVOKE),
    ]
