"""Бухгалтер откатывает утверждение и закрывает часы точки (T115, D036).

**Что чинится.** У роли `accountant` не было `period.reopen` и `unit.close`.
Страница отвечала «Откат периода не входит в права вашей роли», `POST
/periods/<id>/reopen/` — `403`. Это расходилось сразу с тремя местами:

- спека, «Цикл периода», must: «Как **бухгалтер**, хочу откатить утверждение с
  указанием причины, чтобы опечатка не превращалась в разбирательство»;
- D036: «Бухгалтер и оперативный директор имеют РАВНЫЙ доступ». Миграция `0220`
  открыла ей регистры и остановилась на этом — права роли остались прежними;
- D021, объясняющий, зачем откат вообще есть: «одна опечатка в день утверждения
  превращается в разбирательство на месяц». Единственный человек, который в
  сценарии закрытия месяца доходит до утверждения, не мог исправить собственную
  опечатку — ему нужен был директор.

**Почему именно эти два права и не больше.** Открывается ровно то, что следует
из D036: набор бухгалтера становится равен набору оперативного директора, у
которого эти два права уже есть (`unit.close` — с `0110`, D033, по тому же
доводу «запрещать меньшее тому, кому разрешено большее, — не разграничение
доступа, а тупик в работе»). Ничего сверх директорского набора здесь не
появляется, и ни одна другая роль не трогается: управляющего точки решение не
касается (D031) — у него своя точка и два регистра.

**Почему по коду роли, а не по набору прав** — тот же довод, что в `0110` и
`0220`: код роли единственный признак, который спека называет прямо. Партнёр,
назвавший роль иначе, правит её через экран ролей.

**Почему миграция, а не только сид.** Права ролей — данные, и в базе, которая
уже живёт (продукт раскатан), сид больше не гоняется. Без миграции право
появилось бы только у тех, кто пересоздаёт демо-данные, а у остальных бухгалтер
молча остался бы без кнопки.

**Почему в конце стоит проверка.** `roles` под `force row level security`: роль,
не обходящая политики, увидела бы отсюда ноль строк, `update` изменил бы ноль
строк **молча**, миграция «прошла» — а право не появилось (тот же корень, что у
issue #44). Поэтому проверяется результат, а не факт выполнения оператора.
"""
from django.db import migrations

# Добавление, а не переприсваивание: у партнёра набор прав уже мог быть правлен
# экраном ролей, и запись списком стёрла бы его правки.
GRANT = """
update roles
   set permissions = permissions || '["period.reopen", "unit.close"]'::jsonb
 where code = 'accountant'
   and not (permissions ? 'period.reopen' and permissions ? 'unit.close');

do $$
declare
    stuck int;
begin
    select count(*) into stuck
      from roles
     where code = 'accountant'
       and not (permissions ? 'period.reopen' and permissions ? 'unit.close');
    if stuck > 0 then
        raise exception
            'откат и закрытие точки не доехали до % ролей бухгалтера: '
            'строки отрезаны политиками', stuck;
    end if;
end $$;
"""

# Откат снимает ровно эти два права и ровно у этой роли. Это состояние до T115,
# а не «правильное»: откатывают миграцию, чтобы вернуться к коду той же версии,
# и набор прав обязан ехать вместе с ним.
REVOKE = """
update roles
   set permissions = permissions - 'period.reopen' - 'unit.close'
 where code = 'accountant';
"""


class Migration(migrations.Migration):

    dependencies = [
        ("core", "0220_accountant_sees_every_ledger"),
    ]

    operations = [
        migrations.RunSQL(GRANT, REVOKE),
    ]
