"""Сведение двух веток истории схемы: факты и права бухгалтера.

**Почему миграция пустая и всё же обязательна.** Два блока строились
параллельно и каждый вырастил свою миграцию от одного и того же листа `0220`:
`0230_facts` (схема первичных данных, третья очередь) и
`0230_accountant_reopens_and_closes` (права на откат периода и закрытие часов,
следствие D036). Обе правки нужны и не мешают друг другу, но после слияния веток
у истории схемы оказалось **два листа**, а Django отказывается накатывать такую
историю вовсе — падает на `migrate`, а не выбирает один из них.

**Почему это не лечится разведением номеров.** Номер — не зависимость. Даже
если бы вторая называлась `0240`, она всё равно объявляла бы родителем `0220`,
и лист остался бы вторым. Объявить зависимость друг от друга блоки тоже не
могут: пока они работают, ни один не знает, кого примут первым, — а зависимость
от ещё не существующей миграции не пишется.

**Отсюда правило, проверенное на этом проекте четырежды:** развести номера —
работа блоков, свести ветки — работа диспетчера при приёмке. Эта миграция ровно
такое сведение и есть: она ничего не делает со схемой, только объявляет обеих
родителями и возвращает истории один лист.
"""
from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ("core", "0230_facts"),
        ("core", "0230_accountant_reopens_and_closes"),
    ]

    operations = []
