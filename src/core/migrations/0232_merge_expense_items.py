"""Сведение веток: справочник статей расходов и всё остальное.

Пятый случай за прогон и ровно тот же по устройству, что `0231_merge_...`:
блок `cash` вырастил `0231_expense_items` от `0230_facts`, а диспетчер тем
временем свёл в `0231_merge_facts_and_accountant_rights` две другие ветки. Оба
номера оказались `0231` — и это не причина расхождения, а его следствие: номер
не задаёт порядок, порядок задаёт объявленная зависимость.

Блок не мог объявить зависимость правильно: пока он работал, миграции-сведения
ещё не существовало. Поэтому сведение — работа диспетчера при приёмке, и
делается оно вот такой пустой миграцией, которая ничего не меняет в схеме и
только возвращает истории один лист.
"""
from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ("core", "0231_merge_facts_and_accountant_rights"),
        ("core", "0231_expense_items"),
    ]

    operations = []
