"""Сведение веток: роль очереди и разнесение расходов по статьям.

Шестой случай за прогон, и снова тот же: два блока работали параллельно, каждый
вырастил свою миграцию от `0232_merge_expense_items` — `0233_queue_worker_role`
(своя роль базы для рабочего процесса очереди, чтобы разделение ролей на
площадке перестало быть только объявленным) и `0233_allocation_by_expense_item`
(правило разнесения по статье расхода). Обе нужны, друг другу не мешают, и обе
объявили родителем один и тот же лист — значит после слияния листа стало два, а
такую историю Django не накатывает вовсе.

Совпадение номеров тут ни при чём: порядок задаёт объявленная зависимость.
Объявить её правильно блоки не могли — пока они работали, ни один не знал, кого
примут первым, а зависимости от несуществующей миграции не бывает. Поэтому
сведение — работа диспетчера при приёмке, и делается оно вот такой пустой
миграцией.
"""
from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ("core", "0233_allocation_by_expense_item"),
        ("core", "0233_queue_worker_role"),
    ]

    operations = []
