"""Расход на всю сеть — деньги партнёра, а не ничья строка (T130, T131).

Две находки седьмой сверки, обе воспроизведены на раскатанном продукте, и обе
растут из одного корня: **у `unit_id is null` в `facts` появился новый смысл, а
правила остались от старого.**

---

**T130. Управляющий точки видел, правил и удалял расходы юрлица целиком.**

Политика `unit_visibility` на `facts` опирается на `app_unit_is_visible()`
(миграция `0011`), а та первой строкой отвечает:

    select p_unit is null    -- строка без точки: ничья, видна всем

Правило написано в T044, когда фактов без точки в продукте не существовало.
Тогда null у точки значил ровно одно — «точку удалили» (`on delete set null`),
и решение «показать такую строку всем внутри партнёра» было верным: молча
терять осиротевшую строку хуже, чем показать её тому, кто и так работает у
этого партнёра.

С T111 тот же null стал значить совсем другое — «трата юрлица целиком»: аренда
офиса, реклама на сеть. Старое правило встретило новые данные, и «видна всем»
превратилось в «видна и правится каждым»: на демо управляющий одной точки видел
`Marketing campaign 150 000` и `Office rent 90 000`, а итог его списка «для
сверки с кассой» показывал 254 850 вместо 14 850. Правка и удаление такой
строки тоже проходили (999,99 → 1,00 одним POST), и внести расход на всю сеть
он мог тоже.

**Почему не правится сама `app_unit_is_visible`.** Её зовут политики пяти
таблиц: `units`, `employment_terms`, `timesheets`, `payslips` (и через них
`pay_components`), плюс видимость сотрудников (`0020`). Там null у точки
означает по-прежнему «точку удалили», и строгое правило прятало бы осиротевшую
строку ведомости от единственного, кто мог бы заметить пропажу. Один и тот же
null означает в двух местах разное — значит и функций честно две, а не одна с
условием «смотря какая таблица».

Копией условия внутрь политики это тоже не решается: разъехавшиеся копии одного
правила и есть тот способ, которым доступ ломается незаметно (тот же довод, что
в шапке `0011`). Поэтому новое правило — своя функция с именем, которое говорит,
про какой именно null речь, и с одним местом, где оно записано.

**Что теперь видит управляющий.** Родителя-сетевой расход — нет. Разнесённых
детей своей точки — да: это уже строки конкретной точки, они входят в её P&L, и
спрятать их значило бы недосчитать точке её же расходов. Директор и бухгалтер
(D036) видят всё как раньше.

---

**T131. Снятие родителя снимало не всех детей.**

`supersede_fact` собирала детей обычной выборкой, то есть **под политиками
вызвавшего**. Управляющий удалял родителя 100,01 — снимался его ребёнок 33,33,
а 33,34 и 33,34 оставались живыми в `pnl_lines`: 66,68 денег, чей источник
помечен удалённым, и которых не найти ни на одном экране (дети из списка
расходов исключены). Директор то же удаление делал целиком.

Половинчатого исхода быть не должно, и починка здесь из двух половин — они не
взаимозаменяемы:

1. **Полнота.** Список детей собирается `security definer`, то есть не зависит
   от того, кто спрашивает. Дети разнесения — проекция родителя на точки, а не
   самостоятельные строки: родитель снят — снимаются все.
2. **Отказ.** Одной полноты мало: тогда роль, ограниченная своей точкой, молча
   правила бы строки чужих точек. Поэтому снятие родителя, у которого есть
   дети, доступно тому, кто ведёт все точки партнёра, — теми же словами и тем
   же кодом `42501`, что у `allocate_fact` и `reallocate_period` (T111).
   Разносит по точкам тот, кто ведёт все точки; снимает разнесение он же.

После T130 роль с урезанным набором точек до сетевого родителя не дотягивается
вовсе, то есть отказ (2) в продукте не срабатывает ни на одном пути. Он всё
равно нужен: функция записи обязана быть верной сама по себе, а не потому, что
сегодня её некому позвать неправильно. И полнота (1) без него была бы не
починкой, а расширением дыры.

**Почему полнота не «и так есть» при отказе.** Роль, прошедшая отказ, видит все
точки, а регистр у детей наследуется от родителя — значит сегодня она увидела бы
всех детей и обычной выборкой. Это совпадение текущих политик, а не свойство
операции: появится завтра третий разрез видимости (юрлицо, канал), и снятие
снова станет половинчатым — молча. `security definer` делает полноту
устройством функции, а не следствием того, кто её позвал.
"""
from django.db import migrations

# --- T130: чей null ------------------------------------------------------------
FUNCTIONS = """
-- Строка, у которой отсутствие точки означает «вся сеть», а не «точку удалили».
-- Такую строку видит тот, кто ведёт партнёра целиком: это трата юрлица, и для
-- роли с урезанным набором точек она — чужие деньги ровно так же, как расход
-- соседней точки.
--
-- Отдельно от `app_unit_is_visible` намеренно: там тот же null означает
-- осиротевшую строку, и прятать её было бы потерей данных. Два смысла — две
-- функции; общая с условием «смотря какая таблица» была бы тем же самым, только
-- незаметным.
create or replace function app_network_row_is_visible(p_tenant uuid, p_unit uuid)
returns boolean
language sql stable
as $$
    select case
        when p_unit is null then app_unit_ids(p_tenant) is null
        else app_unit_is_visible(p_tenant, p_unit)
    end
$$;

comment on function app_network_row_is_visible(uuid, uuid) is
    'Видна ли строка, у которой отсутствие точки означает «вся сеть»: её видит только тот, кто ведёт все точки';

-- Комментарий соседки приводится в соответствие: с появлением второй функции
-- «видна всем в тенанте» перестало быть общим правилом продукта и стало
-- правилом одного случая — осиротевшей строки.
comment on function app_unit_is_visible(uuid, uuid) is
    'Видна ли строка этой точки. Строка без точки считается осиротевшей (on delete set null) и видна всем в тенанте; для строк уровня сети — app_network_row_is_visible';
"""

DROP_FUNCTIONS = """
drop function if exists app_network_row_is_visible(uuid, uuid);

comment on function app_unit_is_visible(uuid, uuid) is
    'Видна ли строка этой точки текущему пользователю. Строка без точки видна всем в тенанте';
"""

POLICY = """
drop policy if exists unit_visibility on facts;

create policy unit_visibility on facts
    as restrictive for all
    using (app_network_row_is_visible(tenant_id, unit_id))
    with check (app_network_row_is_visible(tenant_id, unit_id));
"""

DROP_POLICY = """
drop policy if exists unit_visibility on facts;

create policy unit_visibility on facts
    as restrictive for all
    using (app_unit_is_visible(tenant_id, unit_id))
    with check (app_unit_is_visible(tenant_id, unit_id));
"""

# --- T131: снятие родителя снимает всех детей ----------------------------------
SUPERSEDE = """
-- Дети разнесения снимаются здесь, а не выборкой в `supersede_fact`: обычная
-- выборка идёт под политиками вызвавшего, и роль с урезанным набором точек
-- снимала бы только своего ребёнка. Разбор — в шапке миграции.
--
-- `security definer` тут не «права побольше», а требование к операции: набор
-- детей у родителя ровно один, и зависеть от того, кто смотрит, он не может.
-- Защита закрытого месяца при этом на месте — `facts_guard` триггер, а триггеры
-- владельца не обходят.
create or replace function supersede_children(p_fact_id uuid)
returns void
language plpgsql security definer
set search_path = public
as $$
declare
    v_tenant uuid;
    c uuid;
begin
    select tenant_id into v_tenant
      from facts
     where parent_fact_id = p_fact_id and superseded_at is null
     limit 1;

    if not found then
        return;    -- детей нет, снимать нечего
    end if;

    -- Снимает разнесение тот, кто ведёт все точки партнёра: строки ложатся на
    -- чужие точки, и роль, ограниченная своей, видит не всё, что снимает. Тот
    -- же довод, те же слова и тот же код, что у `allocate_fact` (T111).
    --
    -- Условие спрашивает и про контекст: без контекста приложения ходит
    -- владелец схемы (обслуживание, миграция данных), у которого список точек
    -- не урезан вовсе.
    if app_user_id() is not null and app_unit_ids(v_tenant) is not null then
        raise exception 'снять разнесение может тот, кто ведёт все точки партнёра'
            using errcode = '42501';
    end if;

    for c in select id from facts
              where parent_fact_id = p_fact_id and superseded_at is null loop
        -- Ссылки на заменившую строку у ребёнка нет намеренно: его заменяет не
        -- другая строка, а исчезновение родителя. Так он и читается в истории —
        -- «удалён», а не «заменён» (T110).
        update facts set superseded_at = now() where id = c;
        perform supersede_children(c);
    end loop;
end $$;

comment on function supersede_children(uuid) is
    'Снять всех детей факта. Список детей не зависит от того, кто спрашивает; роли с урезанным набором точек отказ';

create or replace function supersede_fact(p_fact_id uuid, p_superseded_by uuid default null)
returns void
language plpgsql
as $$
begin
    -- Дети снимаются ПЕРВЫМИ: отказ обязан прийти до того, как что-то записано,
    -- иначе «отказал целиком» держалось бы только на откате транзакции.
    perform supersede_children(p_fact_id);

    -- А сам родитель — под политиками вызвавшего, без прав владельца: функция,
    -- снимающая любой факт по номеру, была бы дырой размером со всю таблицу.
    update facts
       set superseded_at = now(), superseded_by = p_superseded_by
     where id = p_fact_id and superseded_at is null;
end $$;

comment on function supersede_fact(uuid, uuid) is
    'Пометить факт заменённым вместе с его детьми. История остаётся, отчёты её не видят';
"""

BACK_SUPERSEDE = """
create or replace function supersede_fact(p_fact_id uuid, p_superseded_by uuid default null)
returns void
language plpgsql
as $$
declare
    c uuid;
begin
    update facts
       set superseded_at = now(), superseded_by = p_superseded_by
     where id = p_fact_id and superseded_at is null;

    for c in select id from facts where parent_fact_id = p_fact_id and superseded_at is null
    loop
        perform supersede_fact(c);
    end loop;
end $$;

drop function if exists supersede_children(uuid);
"""


class Migration(migrations.Migration):
    dependencies = [("core", "0235_pnl_lines_expense_item")]

    operations = [
        migrations.RunSQL(FUNCTIONS, DROP_FUNCTIONS),
        migrations.RunSQL(POLICY, DROP_POLICY),
        migrations.RunSQL(SUPERSEDE, BACK_SUPERSEDE),
    ]
