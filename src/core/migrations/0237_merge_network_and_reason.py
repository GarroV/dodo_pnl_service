"""Сведение веток: сетевые расходы и честная причина разнесения.

Седьмой случай за прогон, и первый, который успел показать, чем именно он плох.
Два блока починок по седьмой сверке работали параллельно: `net` вырастил
`0236_network_facts` (у «строки без точки» появился новый смысл), `honest` —
`0236_allocation_reason` (продукт называет настоящую причину, по которой расход
не разнесён). Обе объявили родителем `0235_pnl_lines_expense_item`.

Пока лист был один, прогон шёл как обычно. Со вторым листом Django отказывается
строить историю схемы — и это видно не аккуратной ошибкой про миграции, а
**987 упавшими тестами**: любая проверка, которой нужна база, не смогла её
создать. Диагноз по такому выводу читается не сразу, поэтому: при внезапном
падении почти всего набора первым делом смотреть листья
(`MigrationLoader(None).graph.leaf_nodes()`), а не искать общую причину в коде.

Сведение — работа диспетчера при приёмке: блок не знает, кого примут первым, и
объявить зависимость от ещё не существующей миграции не может.
"""
from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ("core", "0236_network_facts"),
        ("core", "0236_allocation_reason"),
    ]

    operations = []
