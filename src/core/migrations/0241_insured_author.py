"""Свой автор у базы для взносов (T156, находка Н6 восьмой сверки).

**Что было.** Подсказка ячейки «База взносов» брала след **строки**
(`timesheets.edited_by`), а его ставит любая правка строки — часов, сдельной
величины, загрузки. Поэтому бухгалтер, поправивший больничный, оказывался
автором числа взносов, которого он не выбирал: база пересчиталась сама
(`store.set_cell` бережёт связь «база идёт за часами»), а имя записалось его.

Это тот же класс, ради которого заведена T143: вопрос «кто поставил это число»
задают ровно тогда, когда числа разошлись, и **неверный ответ хуже
отсутствующего** — его не перепроверяют.

**Почему отдельные колонки, а не запрет писать след строки.** След строки нужен
и верен: строку и правда правил этот человек, и на неё же смотрит колонка
сдельной величины. Отобрать его значило бы потерять ответ там, где он есть.
База для взносов при этом — не производная часов, а самостоятельный вход
расчёта (issue #54, Q005): она законно отличается от отработанного, и спор о
взносах решается вопросом «кто её такой поставил». Своей величине — свой след.

**Пусто — это ответ, и ответов два.** База, посчитанная по часам, автора не
имеет, и экран так и говорит: «посчитано по часам табеля». База, разошедшаяся с
часами и без автора, пришла мимо продукта — «кто поставил, не записано». Разводит
их не эта миграция, а сетка: здесь только место, куда записать имя.

**Почему `uuid` без внешнего ключа.** Тот же приём и та же причина, что у
`edited_by`, `corrected_by` и `closed_by`: `memberships.user_id` намеренно
оставлен голым uuid, чтобы схема не зависела от того, чем окажется вход (D013).

**Обратимость.** Две колонки без значения по умолчанию: ни одной существующей
строки правка не трогает, старый код их не читает, откат сносит только их. У
уже лежащих строк автора нет — и это правда: до сегодня он не записывался.
"""
from django.db import migrations, models

COMMENTS = """
comment on column timesheets.insured_by is
    'Кто задал базу для взносов руками. Пусто — база посчитана по часам табеля либо пришла мимо продукта; автора у неё нет, и выдумывать его нельзя';
comment on column timesheets.insured_at is
    'Когда базу для взносов задали. Пара к insured_by: «кто» без «когда» на первичном документе не отвечает ни на один вопрос';
"""

DROP_COMMENTS = """
comment on column timesheets.insured_by is null;
comment on column timesheets.insured_at is null;
"""


class Migration(migrations.Migration):
    # Зависимость от текущего листа графа: два листа ломают слияние миграций.
    dependencies = [("core", "0240_vat")]

    operations = [
        migrations.AddField(
            model_name="timesheet",
            name="insured_by",
            field=models.UUIDField(blank=True, null=True),
        ),
        migrations.AddField(
            model_name="timesheet",
            name="insured_at",
            field=models.DateTimeField(blank=True, null=True),
        ),
        migrations.RunSQL(COMMENTS, DROP_COMMENTS),
    ]
