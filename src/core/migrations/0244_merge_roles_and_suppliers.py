"""Сведение веток: экран ролей и платежи поставщикам.

Восьмой случай за прогон и первый, случившийся не между двумя блоками, а между
блоком и работой самого диспетчера: `0243_roles_see_people` приехала из
`feat/roles` (экран ролей читает людей своего тенанта), `0243_supplier_lines` —
из `feat/suppliers`. Обе объявили родителем `0242`.

Пока лист один, прогон идёт как обычно. Со вторым листом Django отказывается
строить историю схемы — и видно это не аккуратной ошибкой про миграции, а сотнями
упавших тестов: любая проверка, которой нужна база, не смогла её создать.
Диагноз по такому выводу читается не сразу, поэтому правило прежнее: при
внезапном падении почти всего набора первым делом смотреть листья
(`MigrationLoader(None).graph.leaf_nodes('core')`), а не искать общую причину в
коде.

Сведение — работа диспетчера при приёмке: ветка не знает, кого примут первой, и
объявить зависимость от ещё не существующей миграции не может.
"""
from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ("core", "0243_roles_see_people"),
        ("core", "0243_supplier_lines"),
    ]

    operations = []
