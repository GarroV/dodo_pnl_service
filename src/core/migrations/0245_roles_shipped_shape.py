"""Роль помнит, что ей поставил продукт (T169, issue #126).

**Что чинится.** Форма ролей живёт в коде (`core.roles.ROLE_SHAPES`), а в базе
роли лежат данными, и заводит их сид — один раз, при пересоздании тенанта.
Поэтому правка формы не меняла уже работающую базу ничем: ни миграцией, ни при
старте, ни предупреждением. Дважды это стоило дорого — T089 («администратор
видит 8 сотрудников из 35») и «Расчёт периода не входит в права вашей роли» у
владельца: код был уже прав, стенд остался прежним, и разъезд увидели глазами.

**Что добавляется.** Одна колонка — снимок формы, которую последним поставил
сам продукт. Без неё правку партнёра нельзя отличить от собственного
отставания: видно только «в базе одно, в коде другое». Разбор приёма и четыре
исхода на роль — в `core/role_delivery.py`; здесь только место, куда снимок
кладётся.

**Почему пусто по умолчанию и не заполняется здесь.** Заполнить эту колонку
миграцией значило бы объявить поставленным продуктом всё, что сейчас лежит в
базе, — то есть угадать за партнёра, чего именно он не правил. Продукт на такую
догадку не имеет права: экран `/roles/` живёт с T171, и правки там уже
возможны. Пустой снимок читается как «не знаю» и приводит к исходу, в котором
роль не трогают и показывают человеку. Дописывается снимок сам, ровно там, где
это факт, а не предположение: при пересоздании ролей сидом и при доставке,
которая базу и код и так свела.

**Почему без ограничений на форму значения.** Снимок — служебная запись
продукта о самом себе, а не пользовательские данные: проверять его `check`'ом
значило бы завести второй словарь прав рядом с `web/permissions.py` и
`ROLE_SHAPES`. Мусор в колонке `role_delivery` трактует как отсутствие снимка —
то есть в сторону «не трогать», а не «затереть».

**Политик здесь нет намеренно.** Запись в `roles` уже под правом
`roles.manage` (`0242`), и снимок ходит теми же путями, что остальные колонки
роли. Отдельная политика на одну колонку означала бы третье место, где
описано, кто правит роли.
"""
from django.db import migrations, models

COMMENT = """
comment on column roles.shipped_shape is
    'Снимок формы роли (visible_ledgers + permissions), поставленный самим продуктом: сидом или доставкой core.role_delivery. Пусто — продукт не знает, что стоит в роли, и не трогает её. Нужен, чтобы отличить правку партнёра от отставания базы от кода (T169)';
"""

UNCOMMENT = "comment on column roles.shipped_shape is null;"


class Migration(migrations.Migration):

    dependencies = [
        # Явно от текущего листа: разведённые номера сами по себе от второго
        # листа истории не спасают, а на этом проекте он расходился уже восемь раз.
        ("core", "0244_merge_roles_and_suppliers"),
    ]

    operations = [
        migrations.AddField(
            model_name="role",
            name="shipped_shape",
            field=models.JSONField(blank=True, null=True),
        ),
        migrations.RunSQL(COMMENT, UNCOMMENT),
    ]
