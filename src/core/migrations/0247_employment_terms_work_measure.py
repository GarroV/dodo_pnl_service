"""Чем меряется работа — можно задать человеку, а не только группе (T164, D032).

**Зачем колонка.** «Часовая или окладная» — свойство договора с конкретным
человеком, а не только группы. До этой миграции способ существовал единственным
образом: правилом `groups.<код>.work_measure`, то есть на всю группу сразу и
только правом `rules.manage`. Партнёру, у которого один курьер на фиксированной
выплате, а остальные на часах, оставалось два пути — завести группу из одного
человека либо перевести на фиксированную выплату всех. Оба означают, что продукт
не умеет того, что бухгалтерия делает мышкой в Excel.

**Почему колонка в `employment_terms`, а не переопределение правила на уровне
`employee`.** Технически второй путь уже есть: `rule_overrides` умеет
`scope_type = 'employee'`, и расчёт такие строки применяет. Но правило
адресуется путём `groups.<код>.work_measure`, то есть привязано к **коду
группы**, а не к человеку. Перевод человека в другую группу — обычное дело
(`employment_terms` для этого и версионируются), и переопределение, заведённое
на старый код, после перевода перестало бы применяться: расчёт молча вернулся бы
к мере новой группы. Молчаливая смена способа оплаты — ровно тот класс дефектов,
которого в этом продукте быть не должно.

В условиях найма способ живёт рядом со схемой расчёта и регистром учёта — двумя
другими свойствами «как считается этот человек», которые уже переопределяются
здесь и точно так же: пусто значит «как у группы». Версия по датам у него
получается та же самая, что у ставки: смена способа с 1 сентября не переписывает
август.

**Обратимость.** Обычное добавление необязательной колонки. Ни одной
существующей строки правка не трогает: у всех `null`, а `null` читается как «как
у группы» — то есть расчёт остаётся прежним до копейки. Откат сносит только
колонку.

**Политик не добавляет ни одной, и это не пропуск.** Право на запись в
`employment_terms` уже стоит: `0130_directory_permissions` вешает на таблицу
ограничивающие политики `directory_manage_insert/update/delete`. Новая колонка
той же строки закрыта тем же правом, что и ставка. Заодно это и ответ на «только
правом `rules.manage`» из задачи: мера человека — условие найма, и ведёт её тот,
кто ведёт условия найма.
"""
from django.db import migrations, models

COMMENT = """
comment on column employment_terms.work_measure is
    'Чем меряется работа этого человека (T164, D032): ключ из work_measures правил страны. Пусто — как у группы (правило groups.<код>.work_measure)';
"""

DROP_COMMENT = "comment on column employment_terms.work_measure is null;"


class Migration(migrations.Migration):
    dependencies = [("core", "0246_papers_from_units")]

    operations = [
        migrations.AddField(
            model_name="employmentterm",
            name="work_measure",
            field=models.TextField(blank=True, null=True),
        ),
        migrations.RunSQL(COMMENT, DROP_COMMENT),
    ]
