"""Должность перестаёт повторять группу (вопрос владельца 27.08.2026).

Должность заводилась с тремя полями, которые уже задаёт группа: схема расчёта,
регистр учёта и чем меряется работа. Владелец, читая гайд, спросил про шаги
«Группы» и «Должности»: «разве не об одном и том же?» — и был прав.

Три места на один факт (группа, должность, условия найма) расходятся молча:
завели должность со схемой `standard`, потом поменяли схему группы — и новые
люди считаются иначе, чем старые, а объяснить это нельзя ничем, кроме порядка
заведения.

Разделение теперь такое:

* **группа** — как считаются деньги и куда они попадают в отчёте: схема,
  регистр, мера работы, строка P&L;
* **должность** — кого нанимаем: в какую группу человек попадает, в каких
  границах его ставка и сколько часов у него по договору.

Переопределить схему или регистр конкретному человеку по-прежнему можно — в его
условиях найма, где это и было всегда.
"""

# Generated by Django 6.1 on 2026-08-27 14:14

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('core', '0250_contract_hours'),
    ]

    operations = [
        migrations.RemoveField(
            model_name='position',
            name='ledger',
        ),
        migrations.RemoveField(
            model_name='position',
            name='scheme',
        ),
        migrations.RemoveField(
            model_name='position',
            name='work_measure',
        ),
    ]
