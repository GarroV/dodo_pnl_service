"""
Живое демо: наполненный продукт, открытый без регистрации.

Демо — не второй продукт и не вторая правда о нём. Это **отдельная база с
выдуманными данными** и тонкая точка входа поверх обычных экранов: посетитель
жмёт одну кнопку и оказывается в том же интерфейсе, что и партнёр, только с
людьми, которых не существует.

Три вещи, на которых здесь всё держится.

**Демо-база отдельная, и это проверяется, а не подразумевается (D016).** Продукт
хранит ФИО и суммы живых людей. Демо сбрасывается пересозданием базы целиком,
поэтому «сброс не туда» означал бы стёртые данные партнёра. Отсюда предохранитель
`demo.guard`: любая команда демо сверяет, куда именно она подключена, и
отказывается работать, если это не демо-база.

**Данные выдуманы целиком (D028).** Обезличенная фикстура `plata-sample.xlsx`
для демо не годится: имена в ней сербские, а демо по правилу владельца всегда
англоязычное. Люди, ставки и часы генерируются здесь (`demo.dataset`) —
детерминированно, чтобы каждый сброс давал ровно тот же стенд.

**Демо ничего не пишет в продукт.** Никакой демо-код не вызывается из обычных
представлений: точка входа живёт на своих адресах и включается переменной
`DEMO_MODE`. Выключено — маршрутов нет вовсе, а не «страница отвечает отказом».
"""
