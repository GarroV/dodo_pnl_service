"""Расчёт периода и ведомость.

Движок (`payroll`) считает одного человека по правилам пресета и ничего не знает
про базу. Этот пакет — мост: собирает вход из данных тенанта, зовёт движок,
складывает результат в `payruns/payslips/pay_components` и собирает из них
ведомость для экрана.

Ничего не импортируется на уровне пакета намеренно: сборка ведомости
(`payrun.sheet.assemble`) и выбор пресета (`payrun.rules`) — чистые функции, и
тянуть ради них настройки Django не нужно ни тестам, ни будущим потребителям.

Жизненный цикл периода (статусы, утверждение, откат, ретро-дельта) сюда пока
не входит — это третья очередь, задачи T023 и дальше.
"""
