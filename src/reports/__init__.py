"""Блок `reports`: ведомость, след расчёта, расхождения, сверка, выгрузки.

Здесь живёт **срез данных**, а не их оформление. Экран и выгрузка обязаны
показывать одно и то же: если бы разрез собирался в шаблоне, выгрузку пришлось
бы писать заново, и она разъехалась бы с экраном молча — то есть ровно тем
способом, которого никто не заметит до разговора с бухгалтером.

Django-приложением блок не является: моделей у него нет и не будет. Данные ему
дают `payrun` (результат расчёта) и политики базы (что из этого видно).
"""
