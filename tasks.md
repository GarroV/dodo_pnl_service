<!-- managed by forge: формат строк ниже не менять — файл парсят forge-build и forge-status -->
# Задачи

<!-- id: Tnnn (T001, T002, ...). статус: todo | in_progress | done | failed | blocked:Qnnn (Qnnn — id открытого вопроса из questions.md). "зависит от" — id через запятую или «—». блок: имя блока продукта из plan.md, либо chores для служебных задач, которые не относятся ни к одному блоку (добор исследования, выяснение доступов, ответы владельца) — их не отдают блок-агентам. -->

| id | блок | зависит от | статус | задача |
|---|---|---|---|---|
| T001 | db | T008 | done | Починить изоляцию регистров учёта: политики видимости сделать `as restrictive` и добавить `force row level security`. Дефект подтверждён на живой базе 2026-08-06, фикс написан в `0004_facts.sql` — перенести при переезде на миграции. Готово, когда тест ролью `app_user` показывает: роль с доступом только к `official` не получает строк других регистров |
| T002 | db | T008 | in_progress | Починить видимость записей с `tenant_id is null`: системные роли и общий справочник статей не проходят политику изоляции и не видны никому. Готово, когда тест подтверждает видимость системных ролей и общих статей |
| T003 | db | T008 | in_progress | Убрать привязку к Supabase: перевести функции контекста на `app_user_id()`, удалить шим `auth.uid()` и `db/platform/supabase.sql`. Готово, когда схема накатывается с нуля и `grep -ri supabase db/` пуст |
| T004 | db | T008 | in_progress | Переименовать регистры учёта по D009: тип и колонки `layer` → `ledger`, значения `official`/`supplementary`/`internal` — в миграциях, движке, YAML-пресете и тестах. Готово, когда `grep -ri "white\|grey\|black"` не даёт попаданий по смыслу регистра и тесты зелёные |
| T005 | chores | T008 | todo | Отложено осознанно: дописать тесты SQL-слоя фактов по списку из `docs/backlog-facts.md`. Это модуль сборки P&L, он вне скоупа первой версии и ждёт переезда схемы на миграции Django |
| T006 | chores | — | done | Вычищены персональные данные из истории git: таблица удалена из всех коммитов, имя сотрудника заменено, `.gitignore` пускает только обезличенный файл, оригинал вынесен под `PAYROLL_FIXTURE`. Проверено: `git log -p` не содержит ни одного ФИО из таблицы |
| T007 | chores | T006 | done | Обезличенная фикстура собрана (`tools/make_fixture.py`, 8 листов, 32 строки, все четыре схемы). Регрессия гоняется на ней всегда: 23 зелёных без настоящей таблицы, 31 зелёный с ней |
| T008 | db | — | done | Завести каркас Django 6.1 на Python 3.13 и перенести схему `0001`–`0003` в миграции Django, политики и функции — через `RunSQL`. Готово, когда схема накатывается на чистую базу одной командой и старые SQL-файлы удалены |
| T009 | db | T008 | done | Завести роль `app_user` без `bypassrls` и гранты для неё; тесты доступа гонять только ею. Готово, когда тест изоляции тенантов падает при попытке выполнить его владельцем таблиц — то есть проверяет то, что должен |
| T010 | db | T008 | in_progress | Версионирование правил по датам: непересечение периодов действия через `btree_gist` и `EXCLUDE`. Готово, когда попытка вставить пересекающееся правило отклоняется базой, а тест это показывает |
| T011 | core | T008 | todo | Перенести правила из YAML в таблицы пресетов и переопределений; реализовать `load_preset_at(tenant, country, on_date)` со слоями страна → партнёр → группа → сотрудник. Готово, когда есть тест на каждый уровень переопределения |
| T012 | core | T011 | todo | Сверка переноса: расчёт на правилах из базы даёт то же, что расчёт из YAML, по всем 30 сотрудникам и четырём схемам. Готово, когда тест сравнивает два источника и оба сходятся с эталоном |
| T013 | core | T011 | todo | След расчёта `explain()`: какие правила сработали, с какими входами, какой версией ставки. Готово, когда тест проверяет, что сумма шагов равна итогу строки |
| T014 | auth | T009 | todo | Вход, роли, членства; middleware выставляет контекст пользователя через `set_config(..., true)` внутри транзакции запроса при `ATOMIC_REQUESTS`. Готово, когда четыре роли из спеки работают и представления не фильтруют по тенанту вручную |
| T015 | auth | T014 | todo | Тесты утечки контекста: два запроса разных пользователей на одном соединении пула; фоновая задача без контекста. Готово, когда оба случая дают пустоту, а не чужие данные |
| T016 | web | T014 | todo | Каркас интерфейса: базовый шаблон, навигация, компоненты таблицы с итогами, форм и диалогов; htmx 2.x зафиксированной версии в `static/`. Готово, когда экраны собираются из компонентов, а не из копипасты |
| T017 | web | T016 | todo | Локализация на русский, английский и сербский; демо всегда на английском. Готово, когда переключение языка работает и непереведённых строк на экранах нет |
| T018 | web | T016 | todo | Админка справочников: сотрудники, группы, точки, юрлица, производственный календарь. Готово, когда справочники ведутся без обращения к базе руками |
| T019 | timesheets | T041 | todo | Сетка ввода часов «сотрудник × тип часа» с сохранением ячейки без перезагрузки и клавиатурной навигацией. Готово, когда табель на 30 человек заполняется с клавиатуры и не теряется при уходе со страницы |
| T020 | timesheets | T019 | todo | Импорт таблицы партнёра поверх существующего импортёра, идемпотентный по ключу. Готово, когда загрузка эталонной таблицы даёт корректный табель, а повторная загрузка ничего не меняет |
| T021 | timesheets | T020 | todo | Отчёт импорта и проверки ввода: неизвестные листы и колонки, часов больше нормы, сотрудник без часов, часы у уволенного. Готово, когда испорченный файл не молчит, а показывает, что именно не разобрано |
| T022 | timesheets | T019 | todo | Закрытие часов по точке отдельно от других точек; управляющий видит и правит только свою. Готово, когда есть тест на попытку залезть в чужую точку |
| T023 | payrun | T011, T019 | todo | Модель периода и переходы статусов: черновик → посчитан → утверждён, один черновик на период; запись в утверждённый период отклоняется базой, а не только интерфейсом. Готово, когда есть тест на каждый переход, включая запрещённые |
| T024 | payrun | T023 | todo | Расчёт периода фоновой задачей с прогрессом; задача сама выставляет контекст тенанта. Готово, когда расчёт 30 сотрудников проходит фоном и повторный запуск не плодит ведомости |
| T025 | payrun | T023 | todo | Утверждение и откат: откат требует причины, причина и автор попадают в историю периода. Готово, когда откат без причины невозможен, а история показывает, кто и зачем открывал |
| T026 | payrun | T025 | todo | Правки задним числом: по умолчанию помеченная дельта в текущий период со ссылкой на исходный и в тот же регистр; переключение на пересчёт — только через явное переоткрытие, настройкой тенанта. Готово, когда тест показывает, что закрытый период остался байт в байт прежним |
| T027 | payrun | T023 | todo | Блокировка построчная и по точкам: спорный сотрудник не держит закрытие остальных. Готово, когда тест закрывает точку при незакрытой строке в другой точке |
| T028 | reports | T024 | todo | Ведомость: строка — сотрудник, колонки — компоненты, регистры одной таблицей с меткой и переключателем разреза; итоги считаются по видимому срезу. Готово, когда тест показывает: итоги сходятся с видимыми строками для роли с ограниченным доступом |
| T029 | reports | T013, T028 | todo | Экран следа расчёта: от суммы до входных часов и версии правила. Готово, когда по следу можно воспроизвести сумму руками |
| T030 | reports | T028 | todo | Отчёт расхождений «период против прошлого» с порогами на каждый компонент. Готово, когда тест на намеренно изменённых данных показывает подброшенное отклонение |
| T031 | reports | T028 | todo | Сверка с таблицей бухгалтера: загрузка его файла и построчное сравнение с расчётом. Готово, когда сверка эталонной таблицы июня 2026 показывает совпадение и объясняет известные расхождения |
| T032 | reports | T028 | todo | Три выгрузки: ведомость к выплате, строки для P&L с раздельными налогами, выгрузка в привычном бухгалтеру виде. Готово, когда файлы открываются в Excel и не содержат невидимых пользователю регистров |
| T033 | demo | T024 | todo | Генератор демо-данных: два юрлица, три точки, тридцать сотрудников, все четыре схемы и все три регистра, два закрытых периода и один открытый. Готово, когда в демо работают и отчёт расхождений, и сверка |
| T034 | demo | T033 | todo | Демо-база отдельным экземпляром, вход без регистрации, сброс пересозданием из шаблона по расписанию. Готово, когда сброс проходит без ручных действий и запись в демо не попадает в боевую базу |
| T035 | deploy | T008 | done | Локальный запуск: compose, `.env.example`, README с поднятием с нуля. Готово, когда чистая машина поднимает продукт по инструкции без вопросов |
| T036 | deploy | T035, T016 | todo | Развернуть на MUSPELHEIM своим compose-проектом на портах 8030 и 5440, база с меткой для ночного бэкапа. Готово, когда смоук проходит **на сервере**, соседние контейнеры живы, а свежий дамп появился |
| T037 | deploy | T035 | todo | CI на каждый pull request: линт, тесты, сборка образа. Готово, когда падающий тест блокирует слияние |
| T038 | db | T008 | done | Сид тестовых данных для разработки: 32 выдуманных сотрудника из обезличенной фикстуры, точки и юрлица, один открытый период. До готового MVP настоящая таблица партнёра не загружается (D028). Готово, когда `manage.py seed_dev` наполняет чистую базу и расчёт проходит на ней |
| T039 | chores | T032 | todo | Приёмка MVP на настоящих данных: загрузить таблицу бухгалтерии июня 2026 в поднятый продукт и сверить с её же расчётом. Делается один раз, в конце, а не по ходу. Готово, когда ведомость в продукте совпадает с эталоном |
| T040 | chores | T028 | todo | Заложить MCP-обёртку поверх HTTP API как отдельную задачу на будущее: в первую версию не входит (D030), но эндпоинты проектируются так, чтобы обёртка не требовала переписывания. Готово, когда в спеке API зафиксировано, какие вызовы MCP будет использовать |
| T041 | web | T008, T038 | done | Минимальный веб-каркас первой очереди: проект Django поднимается, dev-вход без регистрации, список периодов, страница периода. Без локализации, ролей и оформления — они в следующих очередях. Готово, когда страница периода открывается в браузере на данных сида |
| T042 | payrun | T041 | done | Расчёт периода из интерфейса: кнопка запускает движок на данных сида (правила пока из YAML, перенос в базу — вторая очередь) и показывает ведомость: строка на сотрудника, колонки на компоненты, итоги. Готово, когда месяц считается в браузере и суммы совпадают с прямым вызовом движка |
| T043 | chores | T042 | done | Сверить построенное первой очередью со спекой свежим взглядом, итерация 1: агент, который не строил, проходит сценарии как пользователь и проверяет критерии готовности. Каждое расхождение — отдельной задачей |
| T044 | web | T014 | todo | Управляющий точки видит все точки: интерфейс обещает «точка NS1», а ведомость показывает все три — фильтра по `unit_ids` нет нигде. Либо фильтровать, либо не обещать. Найдено сверкой 2026-08-07 |
| T045 | db | — | in_progress | Внутренний регистр не представлен в сиде ни одной строкой, поэтому сценарий его скрытия сид сам по себе не проверяет — при сверке пришлось вставлять строку руками. Добавить в `seed_dev` данные всех трёх регистров |
| T046 | deploy | — | todo | Привести документы деплоя в соответствие: таблица переменных в `deploy.md` неполна (нет `DEV_LOGIN`, `DJANGO_DEBUG`, `DJANGO_ALLOWED_HOSTS`, `DODO_TEST_ADMIN_DSN`, `HEALTHCHECK_*`), в README нет таблицы переменных, которую требует конституция, а `healthcheck.py` и `deploy.md` расходятся в том, что отдаёт корень |
| T047 | db | T010 | in_progress | `db.md` заявляет действующей гарантией непересечение периодов действия через `EXCLUDE`, а в схеме нет ни одного такого ограничения — гарантия описана, но не выполняется. Убрать из контракта до реализации либо сделать вместе с T010 |
| T048 | chores | — | todo | `db/platform/postgres.sql` ссылается на удалённые `0001–0003.sql`, а «Порядок работ» в `CLAUDE.md` разъехался с `tasks.md` — два параллельных плана, один устарел. Свести к одному источнику |
| T049 | web | — | todo | Шапка показывает бейдж «dev-вход» и ссылку на страницу входа даже при `DEV_LOGIN=0`, ссылка ведёт в 404: шаблон не смотрит на флаг |
| T050 | db | T004 | in_progress | У `payslips` нет политики видимости регистров: поля `net`, `gross`, `tax`, `to_cash` посчитаны по всем регистрам сразу, и скрытый регистр восстанавливается вычитанием из видимых компонентов (issue #42). Сейчас обойдено дисциплиной в коде — ведомость собирается только из компонентов. Решить в базе: суммы в разрезе регистра либо отказ от суммарных полей. Готово, когда тест ролью `app_user` показывает: бухгалтер не может получить из `payslips` число больше суммы видимых ему компонентов |
| T051 | db | T008 | in_progress | В `timesheets` негде хранить выплату наличными и ручную корректировку — движок принимает `cash_payout` и `manual_correction`, в схеме колонок нет, поэтому `to_cash` всегда 0, а сид теряет значения (issue #43). Корректировка со следом: кто и почему поправил (D025). Готово, когда расчёт периода с заданной корректировкой даёт компонент и пометку в `notes`, а выплата наличными попадает в `to_cash` |
