<!-- managed by forge: формат строк ниже не менять — файл парсят forge-build и forge-status -->
# Задачи

<!-- id: Tnnn (T001, T002, ...). статус: todo | in_progress | done | failed | blocked:Qnnn (Qnnn — id открытого вопроса из questions.md). "зависит от" — id через запятую или «—». блок: имя блока продукта из plan.md, либо chores для служебных задач, которые не относятся ни к одному блоку (добор исследования, выяснение доступов, ответы владельца) — их не отдают блок-агентам. -->

| id | блок | зависит от | статус | задача |
|---|---|---|---|---|
| T001 | db | — | todo | Починить изоляцию регистров учёта: политики `layer_visibility` в `0003_rls.sql` пермиссивные, складываются с `tenant_isolation` через OR и потому не ограничивают ничего. Дефект подтверждён на живой базе, фикс написан в `0004_facts.sql` — перенести его при переезде на миграции Django, добавить `force row level security`. Готово, когда есть тест: пользователь с ролью, видящей только `official`, не получает строк других регистров |
| T002 | db | — | todo | Та же дыра в `roles`: `tenant_id` nullable, «системные» роли не проходят политику `tenant_id in (...)` и не видны никому. Сейчас незаметно, потому что функция видимости читает роли в обход RLS. Готово, когда экран управления ролями показывает системные роли, а тест это подтверждает |
| T003 | db | — | todo | Перевести `0003_rls.sql` с `auth.uid()` на `app_user_id()` и удалить шим `auth.uid()` вместе с `db/platform/supabase.sql`: платформа выбрана (свой Postgres на MUSPELHEIM, D006/D007), Supabase не используется. Готово, когда схема накатывается с нуля без единого упоминания Supabase |
| T004 | db | — | todo | Переименовать регистры учёта по D009: тип `accounting_layer` и колонки `layer` → `ledger` со значениями `official`/`supplementary`/`internal`, во всех миграциях, в движке, в YAML-пресете и в тестах. Готово, когда `grep -ri "white\|grey\|black" src db tests` не даёт попаданий по смыслу регистра, а тесты зелёные |
| T005 | facts | T003 | todo | Дописать тесты SQL-слоя фактов по списку из `docs/backlog-facts.md` (разнесение, идемпотентность, ретро-пересчёт, закрытый период, изоляция, сборка P&L). Отложено осознанно: это модуль сборки P&L, вне скоупа первой версии, и он ждёт переезда схемы на миграции Django |
