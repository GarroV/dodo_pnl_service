"""
Включает измерение покрытия в подпроцессах.

Тесты схемы и сида гоняют `manage.py` настоящим подпроцессом — иначе проверялось
бы не то, что запустит человек. Но coverage видит только свой процесс, поэтому
без этого хука миграции и команда сида показывали бы 0% при том, что каждая
строка отработала. Каталог попадает в PYTHONPATH подпроцесса только когда задан
COVERAGE_PROCESS_START (см. run_manage в conftest).
"""
import coverage

coverage.process_startup()
